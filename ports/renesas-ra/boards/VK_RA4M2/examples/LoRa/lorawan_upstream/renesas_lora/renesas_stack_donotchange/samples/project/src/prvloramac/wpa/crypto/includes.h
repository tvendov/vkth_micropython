#include <string.h>

#define os_strlen(label)      strlen(label)
#define os_memcpy(a, b, c)    memcpy(a, b, c)
#define os_memset(a, b, c)    memset(a, b, c)

#define __must_check

typedef unsigned char     u8;
typedef unsigned short    u16;



