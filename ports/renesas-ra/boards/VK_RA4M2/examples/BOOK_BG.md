# Книга: MicroPython за VK_RA4M2 чрез демонстрационни примери

## Цел

Тази книга е направена върху реално наличния пакет от примерни `.py` файла за `VK_RA4M2`.

Тук целта не е само да се изброят файловете.

Целта е да се покрият всички глави, теми и примери от плана, без пропуски и без измислени ресурси.

Затова книгата е организирана в три слоя:

1. Ресурсна карта на борда и порта.
2. Матрица за пълност спрямо учебния план.
3. Подробни глави с кодови откъси и препратки към всички реални сорс файлове.

## Как да се ползва книгата

Книгата се чете заедно със сорс файловете в `examples`.

Препоръчителният ритъм е:

1. Прочитате теорията в съответната глава.
2. Стартирате показания минипример.
3. Отваряте пълния `.py` файл.
4. Преглеждате ресурсния header в началото му.
5. Променяте само по една стойност и наблюдавате ефекта.

Когато примерът съдържа предпазен флаг като `DO_WRITE`, `DO_BOOTLOADER`, `DO_LIGHTSLEEP`, `DO_DEEPSLEEP` или `SET_DEMO_TIME`, започвайте с безопасната стойност.

## Съдържание

Тази секция дава бърз преглед на всички части и глави в книгата.

### Част I: Първи стъпки с борда и с MicroPython
- Глава 1. Първа програма
- Глава 2. Първи асинхронни примери

### Част II: Езикови основи на MicroPython
- Глава 3. Променливи и константи
- Глава 4. Оператори, условия и цикли
- Глава 5. Enums в MicroPython
- Глава 6. Масиви и таблици
- Глава 7. Структури в MicroPython
- Глава 8. Указатели и еквивалентите им в MicroPython
- Глава 9. Функции, callbacks и таблици от функции

### Част III: Цифрови и аналогови входове и изходи
- Глава 10. Цифров изход
- Глава 11. Цифров вход
- Глава 12. Аналогов вход и ADC
- Глава 13. PWM

### Част IV: Таймери, прекъсвания и време
- Глава 14. Закъснения, хардуерни таймери и `Timer(-1)`
- Подглава 14.1. Хардуерни таймери: `periodic`, `one-shot`, `counter`, `period`, `output compare`, `input capture`, `event count`
- Подглава 14.2. Практически checklist за AGT тестове
- Глава 15. Прекъсвания
- Глава 16. Дебаунс на бутон
- Глава 17. Четене на бутон: `polling`, IRQ, `Timer(-1)`, debounce и събития
- Глава 18. RTC

### Част V: Серийни интерфейси
- Глава 19. I2C master и runtime избор на пинове
- Глава 20. SoftI2C и I2CTarget
- Глава 21. Hardware SPI и SoftSPI

### Част VI: TouchPad, памет и системни функции
- Глава 22. TouchPad: базово четене
- Глава 23. TouchPad: диагностика, cached API и асинхронно следене
- Глава 24. `renesas.Flash`, `/flash` и `dataflash`
- Глава 25. Управление на паметта в MicroPython
- Глава 26. `machine` модулът и порт-специфично поведение

### Част VII: Индикация и входни матрици
- Глава 27. Динамична 7-сегментна индикация и матрично сканиране

### Част VIII: FSM като начин на мислене
- Глава 28. Минимална FSM и таблична FSM
- Глава 29. FSM с timeout
- Глава 30. Conditions vs Events
- Глава 31. Guard функции
- Глава 32. Приоритети и event queue
- Глава 33. Tickless timers и sleep идея

### Част IX: Реален пример
- Глава 34. Управление на осветление и вентилация за баня
- Глава 35. Декомпозиция на две FSM

### Част X: UART комуникация
- Глава 36. Базова UART комуникация

### Част XI: Външни модули по интерфейси
- Глава 37. Работа с външни модули по интерфейси

### Част XII: asyncio като учебна секция
- Глава 38. Основи на asyncio
- Глава 39. asyncio Event синхронизация

### Част XIII: Специализирани светлинни модули
- Глава 40. NeoPixel
- Глава 41. WS2812 през `machine.WS2812` и SCI TX-only

### Част XIV: Звук, DAC и retro synth
- Глава 42. DAC audio, timed playback и retro synth

### Част XV: Надеждни вградени програми
- Глава 43. `boot.py`, `main.py` и жизнен цикъл на програмата
- Глава 44. Организация на проект: модули, drivers, config и `pins.py`
- Глава 45. Байтове, `bytearray`, `struct`, `memoryview` и двоични протоколи
- Глава 46. Обработка на грешки, recovery и fail-safe поведение
- Глава 47. Watchdog и самовъзстановяване

### Част XVI: Сигнали, измерване и калибрация
- Глава 48. Филтриране, калибриране и хистерезис при входни сигнали
- Глава 49. Измерване и диагностика с осцилоскоп и logic analyzer
- Глава 50. Ниска консумация и събуждане

### Част XVII: Архитектура на реален проект
- Глава 51. Състояние на системата: heartbeat, health checks и debug интерфейс
- Глава 52. Капсуловане на периферии като класове
- Глава 53. Таблични данни, конфигурации и малки файлови формати
- Глава 54. От пример към завършено приложение

## Ресурсна карта на VK_RA4M2

Книгата и примерите приемат следната карта на ресурси:

- LED: `1 брой` -> `LED1 = P204`
- Бутон: `1 брой` -> `SW1 = P400`
- USB device пинове: `USBDP = P914`, `USBDM = P915`, `USB_VBUS = P407`
- ADC външни входове: `13 броя` -> `P000`, `P001`, `P002`, `P003`, `P004`, `P005`, `P006`, `P007`, `P008`, `P013`, `P014`, `P015`, `P500`
- ADC вътрешни източници: `3 броя` -> `ADC.CORE_TEMP`, `ADC.CORE_VREF`, `ADC.VREF`
- DAC изходи: `2 броя` -> `P014`, `P015`
- PWM изходи: `14 броя` -> `P107`, `P106`, `P105`, `P104`, `P113`, `P114`, `P112`, `P115`, `P608`, `P409`, `P408`, `P600`, `P304`, `P303`
- UART: `4 инстанции` -> `UART(0)`, `UART(2)`, `UART(7)`, `UART(9)`
- I2C master: `2 инстанции` -> `I2C(0)=P400/P401`, `I2C(1)=P100/P101`
- I2CTarget: `2 инстанции` -> `I2CTarget(0)=P400/P401`, `I2CTarget(1)=P100/P101`
- SPI: `1 канал` -> `CS=P103`, `SCK=P102`, `MISO=P100`, `MOSI=P101`
- SoftI2C: наличен
- SoftSPI: наличен
- TouchPad входове: `12 броя` -> `P205`, `P206`, `P407`, `P408`, `P409`, `P410`, `P411`, `P412`, `P413`, `P414`, `P415`, `P708`
- CTSU специален пин: `P207 = TSCAP`
- Хардуерни таймери: `6 броя` -> `Timer(1)`, `Timer(2)`, `Timer(3)`, `Timer(4)`, `Timer(5)`, `Timer(6)`
- Софтуерен таймер: `Timer(-1)`
- RTC: `1 брой`
- Вътрешна файлова система: `/flash`
- `renesas.Flash`: наличен block device
- `dataflash`: `8 KB`

Важни уточнения:

- `SPI` тук се разглежда като `1` канал.
- `Timer(-1)` тук се разглежда като софтуерен таймер.
- `SW1` е върху `P400`, което влиза в конфликт с `I2C(0).SCL`.
- `LED1` е active-low: логическа `0` го светва, логическа `1` го гаси.
- `Pin.PULL_UP` се ползва в курса; `Pin.PULL_DOWN` не се разглежда като наличен ресурс в тези уроци.
- `lightsleep` и `deepsleep` са показани предпазливо.
- `J16` е boot-mode jumper, не е DAC изход.
- `DAC` timed playback върху `P014/P015` е потвърден на реален борд за `DTC` circular, `DTC` one-shot и `DMAC` one-shot.
- `DAC` пинът подава аналогов сигнал, а не мощност към товар. За слушане ползвайте активна колона, усилвател или осцилоскоп, не директно гол говорител.

## Матрица за пълност спрямо плана

Това е контролният списък, по който книгата е проверена.

- `Примерна програма` -> `examples/20_language_basics/01_sample_program.py`
- `Променливи и константи` -> `examples/20_language_basics/02_variables_and_constants.py`
- `Аритметични оператори` -> `examples/20_language_basics/03_operators_conditions_loops.py`
- `Логически оператори` -> `examples/20_language_basics/03_operators_conditions_loops.py`
- `if / if-else / if-elif-else` -> `examples/20_language_basics/03_operators_conditions_loops.py`
- `goto в MicroPython липсва` -> обяснено в главата за управление на изпълнението
- `switch-case в MicroPython липсва` -> обяснено в главата за управление на изпълнението
- `while` -> `examples/20_language_basics/03_operators_conditions_loops.py`
- `for` -> `examples/20_language_basics/03_operators_conditions_loops.py`
- `do-while еквивалент` -> `examples/20_language_basics/03_operators_conditions_loops.py`
- `Цифров изход` -> `examples/21_digital_io/01_digital_output.py`, `examples/01_blink_async.py`
- `Цифров вход` -> `examples/21_digital_io/02_digital_input.py`
- `Аналогов вход` -> `examples/22_analog/01_analog_input_adc.py`
- `Какво е ADC и как работи` -> обяснено в главата за ADC и показано в `examples/22_analog/01_analog_input_adc.py`
- `Измерване на ADC.read() с time.ticks_us()` -> `examples/23_timing/07_adc_timing_measure.py`
- `PWM изход` -> `examples/22_analog/02_pwm_output.py`, `examples/03_pwm_ab_same_channel.py`
- `DAC изход` -> обяснено в главата за DAC audio и показано в `examples/retro_synth.py`, `examples/dac_retro_sfx.py`, `examples/dac_retro_music.py`
- `Таймер за семплиране и хардуерен пренос към DAC` -> обяснено в главата за DAC audio и валидирано с `examples/dac_firmware_probe.py`
- `Retro synth, wavetable loop и звукови ефекти` -> `examples/retro_synth.py`, `examples/dac_retro_sfx.py`, `examples/dac_retro_music.py`
- `Enums` -> `examples/20_language_basics/04_enums.py`
- `Масив` -> `examples/20_language_basics/05_arrays.py`
- `Масив от масиви` -> `examples/20_language_basics/05_arrays.py`
- `Структури` -> `examples/20_language_basics/06_structures.py`
- `Структури от структури` -> `examples/20_language_basics/06_structures.py`
- `Масив от структури` -> `examples/20_language_basics/06_structures.py`
- `Указатели като идея` -> `examples/20_language_basics/07_pointers_equivalents.py`
- `Указател към променлива/масив/структура` -> еквиваленти чрез references и mutable обекти в `examples/20_language_basics/07_pointers_equivalents.py`
- `Указател към указател` -> обяснено като верига от references и индирекция чрез mutable обекти
- `Масив от указатели` -> еквивалент чрез списък от функции в `examples/20_language_basics/08_functions.py`
- `Управление на паметта в MicroPython` -> `examples/28_machine_misc/04_memory_management.py`, `examples/20_language_basics/07_pointers_equivalents.py`, `examples/24_i2c/04_i2ctarget_memory.py`
- `gc.collect()`, `gc.mem_free()`, `gc.mem_alloc()`, `micropython.mem_info()` -> `examples/28_machine_misc/04_memory_management.py`
- `memoryview` без копиране и преизползване на буфер -> `examples/20_language_basics/07_pointers_equivalents.py`, `examples/28_machine_misc/04_memory_management.py`, `examples/24_i2c/04_i2ctarget_memory.py`
- `Функция` -> `examples/20_language_basics/08_functions.py`
- `Входни и изходни параметри` -> `examples/20_language_basics/08_functions.py`
- `Параметри чрез указател` -> еквивалент чрез mutable обект в `examples/20_language_basics/08_functions.py`
- `Указател към функция` -> callback reference в `examples/20_language_basics/08_functions.py`
- `Масив от указатели към функции` -> `operation_table` в `examples/20_language_basics/08_functions.py`
- `Таймери и закъснения` -> `examples/23_timing/01_delays_and_timers.py`
- `Софтуерен таймер` -> `examples/23_timing/04_software_timer_minus1.py`
- `Хардуерен срещу софтуерен таймер` -> `examples/23_timing/05_timer_compare_hw_sw.py`
- `Хардуерен periodic и one-shot таймер` -> `examples/23_timing/13_hardware_timer_periodic_oneshot.py`
- `Timer.period()` и `Timer.counter()` при хардуерен таймер -> `examples/23_timing/13_hardware_timer_periodic_oneshot.py`
- `Хардуерен output compare callback` -> `examples/23_timing/14_hardware_timer_output_compare.py`
- `Хардуерен input capture: период и pulse width` -> `examples/23_timing/15_hardware_timer_input_capture.py`
- `Хардуерен event count и callback на всеки N импулса` -> `examples/23_timing/16_hardware_timer_event_count.py`
- `time.ticks_us()` и относителни измервания -> `examples/23_timing/07_adc_timing_measure.py`
- `Прекъсвания` -> `examples/23_timing/02_interrupts.py`
- `Измерване на interrupt latency` -> `examples/23_timing/08_interrupt_latency_measure.py`
- `Приоритети` -> `examples/30_fsm/06_priorities.py`
- `Забраняване/разрешаване на прекъсвания` -> `examples/23_timing/02_interrupts.py`, `examples/28_machine_misc/02_machine_mem_and_bootloader.py`
- `Четене на бутон и дебаунс` -> `examples/23_timing/03_button_debounce.py`
- `Четене на бутон с polling` -> `examples/23_timing/09_button_polling_events.py`
- `Четене на бутон по прекъсване с отложена обработка` -> `examples/23_timing/10_button_irq_deferred.py`
- `Четене на два бутона с fast ASM IRQ и shared queue` -> `examples/23_timing/17_fast_irq_two_buttons_queue.py`
- `Четене на бутон със софтуерен Timer(-1)` -> `examples/23_timing/11_button_soft_timer_hold.py`
- `Събитие при натискане и отпускане` -> `examples/23_timing/09_button_polling_events.py`, `examples/23_timing/10_button_irq_deferred.py`
- `Събитие при задържане` -> `examples/23_timing/09_button_polling_events.py`, `examples/23_timing/11_button_soft_timer_hold.py`
- `HOLD_START и HOLD_REPEAT` -> `examples/23_timing/11_button_soft_timer_hold.py`
- `Дебаунс с битово изместване` -> `examples/23_timing/12_button_shift_debounce.py`
- `Динамична 7-сегментна индикация` -> `examples/29_display_scan/01_dynamic_7seg_and_keypad.py`
- `Прекъсвания по таймер / времево опресняване` -> обяснено в главата за display scan и timer главите
- `Матрично сканиране на бутони` -> `examples/29_display_scan/01_dynamic_7seg_and_keypad.py`
- `FSM идея` -> `examples/30_fsm/01_fsm_lamp_button.py`
- `Графично и таблично описание на FSM` -> обяснено в главата за FSM и показано в `examples/30_fsm/02_fsm_table.py`
- `FSM с timeout` -> `examples/30_fsm/03_fsm_timeout.py`
- `Conditions vs Events` -> `examples/30_fsm/04_conditions_vs_events.py`
- `Guard функции` -> `examples/30_fsm/05_guard_functions.py`
- `Реален пример: баня` -> `examples/31_case_study/01_bathroom_control.py`
- `Декомпозиция на 2 FSM` -> `examples/31_case_study/02_two_fsms.py`
- `Комуникация между стейт машини` -> `examples/31_case_study/02_two_fsms.py`
- `Event queue` -> `examples/30_fsm/07_event_queue.py`
- `Tickless timers + sleep` -> `examples/30_fsm/08_tickless_sleep.py`, `examples/28_machine_misc/03_sleep_modes.py`
- `Работа с външни модули по интерфейси` -> обяснено в главата за външни модули и показано в `examples/34_external_modules/01_i2c_module_probe.py`, `examples/34_external_modules/02_spi_module_transaction.py`, `examples/34_external_modules/03_uart_module_request_response.py`
- `I2C scan и безопасен probe на модул` -> `examples/34_external_modules/01_i2c_module_probe.py`
- `SPI транзакция с CS рамка` -> `examples/34_external_modules/02_spi_module_transaction.py`
- `UART request/response с timeout` -> `examples/34_external_modules/03_uart_module_request_response.py`
- `NeoPixel индикация` -> `examples/neopixel.py`, `examples/neopixel_test.py`
- `Захранване на малък външен RGB модул от GPIO и еднопроводен data канал` -> обяснено в главата за NeoPixel и показано в `examples/neopixel.py`
- `WS2812 през специализиран C драйвер` -> `examples/ws2812_sci.py`, `examples/ws2812_sci_test.py`, `examples/ws2812_sci_p101.py`, `examples/ws2812_sci_p109.py`
- `WS2812 върху валиден SCI TX/MOSI pin без външен clock pin към модула` -> обяснено в главата за `machine.WS2812`

## Част I: Първи стъпки с борда и с MicroPython

### Глава 1. Първа програма

Първата програма трябва да даде веднага видим резултат и едновременно да покаже една от най-важните board-специфични особености на `VK_RA4M2`: `LED1` е active-low.

Ключова идея:

```python
from machine import Pin  # Импортираме класа Pin, чрез който управляваме GPIO пиновете.
import time  # Импортираме модула time, за да правим закъснения между действията.

led = Pin("LED1", Pin.OUT, value=1)  # Създаваме изходен обект за LED1 и започваме с изгасен LED.

for blink_index in range(3):  # Обхождаме три последователни мигания с for цикъл.
    led.value(0)  # Светваме LED1 като подадем активното за платката ниво.
    time.sleep_ms(300)  # Изчакваме 300 милисекунди, за да се вижда светването.
    led.value(1)  # Гасим LED1, за да завършим едно мигане.
    time.sleep_ms(300)  # Изчакваме още 300 милисекунди преди следващото мигане.
```

Тук се виждат наведнъж:

- `Pin`
- `for`
- `sleep_ms`
- active-low поведението на `LED1`

Файлове в главата:

- `examples/20_language_basics/01_sample_program.py`

#### Резюме
- Главата показва най-малкия работещ MicroPython пример за платката.
- Фокусът е върху `Pin`, `sleep_ms()` и active-low поведението на `LED1`.
- Това е отправната точка за всички следващи практически уроци.

#### Самопроверка
1. Защо `LED1` светва при `value(0)`?
2. Каква е ролята на `Pin.OUT` и `sleep_ms()` в примера?
3. Коя е най-малката промяна, с която бихте проверили, че разбирате кода?

#### Упражнения
1. Променете броя мигания и дължината на паузата и наблюдавайте резултата.
2. Направете различни времена за светнато и изгасено състояние.
3. Изнесете броя мигания и закъснението в именувани константи.

#### Ако не работи
- Симптом: LED1 не мига. Проверка: използвайте `Pin("LED1", Pin.OUT)` и имайте предвид active-low логиката.
- Симптом: ефектът е невидим. Проверка: вероятно паузите са твърде кратки или цикълът не се изпълнява както очаквате.

#### Нататък
След тази глава преминете към „Първи асинхронни примери“, за да видите как същата платка може да прави повече от едно нещо без блокиращ цикъл.

### Глава 2. Първи асинхронни примери

Преди да влезем в детайлите на езика, е полезно да се види как изглежда неблокиращото мислене на практика: един LED може да мига, а друга задача да брои време, без едната да спира другата.

Минипример:

```python
import asyncio  # Импортираме asyncio, за да правим неблокиращи задачи в MicroPython.
from machine import Pin  # Импортираме класа Pin, за да управляваме LED1 като цифров изход.

led = Pin("LED1", Pin.OUT, value=1)  # Създаваме изход за LED1 и започваме от изгасено състояние.

async def blink_task():  # Дефинираме асинхронна задача, която ще мига без да блокира event loop-а.
    while True:  # Повтаряме мигането безкрайно.
        led.value(0)  # Светваме LED1.
        await asyncio.sleep_ms(500)  # Отстъпваме управлението за 500 ms.
        led.value(1)  # Гасим LED1.
        await asyncio.sleep_ms(500)  # Отново отстъпваме управлението за 500 ms.
```

Втори минипример:

```python
async def count_seconds_task():  # Създаваме втора задача, която брои секундите.
    seconds = 0  # Началната стойност на брояча е нула секунди.
    while True:  # Повтаряме и тази задача безкрайно.
        seconds += 1  # Увеличаваме брояча с една секунда.
        print("Изминали секунди:", seconds)  # Показваме колко време е минало от старта.
        await asyncio.sleep(1)  # Чакаме точно една секунда без блокиране на event loop-а.
```

Тези примери показват две идеи:

- `asyncio` като кооперативен scheduler
- разделяне на поведението на независими задачи

Файлове в главата:

- `examples/01_blink_async.py`
- `examples/02_two_tasks.py`

#### Резюме
- Главата въвежда кооперативното мислене с `asyncio`.
- Показва се как две задачи могат да споделят процесора без взаимно блокиране.
- Това е основа за неблокиращи приложения върху борда.

#### Самопроверка
1. Какво прави `await` в асинхронната задача?
2. Защо една async задача не бива да работи без да отстъпва управлението?
3. Каква е практическата разлика между две async задачи и един блокиращ цикъл?

#### Упражнения
1. Добавете трета async задача с различен период.
2. Променете периодите на задачите и наблюдавайте как се преплитат.
3. Добавете брояч, който спира демонстрацията след определено време.

#### Ако не работи
- Симптом: виждате поведение само от една задача. Проверка: търсете липсващ `await` или неправилно създаване на задачите.
- Симптом: програмата не тръгва асинхронно. Проверка: уверете се, че event loop-ът се стартира по правилния начин.

#### Нататък
След тази глава преминете към „Променливи и константи“, за да подредите езиковите елементи, които после ще използвате и в асинхронния код.

## Част II: Езикови основи на MicroPython

### Глава 3. Променливи и константи

Пример:

```python
from micropython import const  # Импортираме const, за да дефинираме истински MicroPython константи.

BOARD_NAME = "VK_RA4M2"  # Това е обикновена низова променлива с името на платката.
LED_ALIAS = "LED1"  # Това е още една обикновена променлива, която пази board alias на LED-а.
BLINK_COUNT = const(3)  # Това е константа, която няма да променяме по време на работа.
BLINK_DELAY_MS = const(200)  # Това е константа за закъснение в милисекунди.
uses_asyncio = True  # Булева променлива ни показва дали платката има поддръжка на asyncio.
```

Тук разграничението е просто:

- променлива е нещо, което променяме
- `const()` е именувана константа, с която описваме числа и режими

Файл:

- `examples/20_language_basics/02_variables_and_constants.py`

#### Резюме
- Главата въвежда темата „Променливи и константи“ чрез кратък изпълним MicroPython пример.
- Идеята е показана в чист синтаксис, без да се губи връзката с бъдещите хардуерни примери.
- Това е езиков строителен блок за по-сложните глави нататък.

#### Самопроверка
1. Каква е разликата между обикновена променлива като `BOARD_NAME` и константа като `BLINK_COUNT = const(3)`?
2. Защо е по-добре важните числови настройки да имат име, вместо да ги пишете като „голи числа“ в кода?
3. Къде в бъдещ хардуерен пример бихте използвали константа за pin, delay или режим?

#### Упражнения
1. Изнесете още две стойности от предишния LED пример в константи, например брой мигания и закъснение.
2. Добавете именувана константа за режим, например `MODE_BLINK = const(1)`, и я използвайте в кратък `if`.
3. Пренапишете малък пример така, че всички константи да са с главни букви, а всички променливи с малки.

#### Ако не работи
- Симптом: имената в кода вече не ви подсказват какво правят числата. Проверка: превърнете важните стойности в добре именувани константи.
- Симптом: `const()` не дава очакваната полза. Проверка: използвайте го основно за числови константи и не го бъркайте с обикновена променлива.

#### Нататък
След тази глава преминете към „Оператори, условия и цикли“, за да започнете да работите не само със стойности, но и с логика и управление на изпълнението.

### Глава 4. Оператори, условия и цикли

Тази глава покрива аритметика, сравнения, логика и контрол на потока.

Пример:

```python
left_value = 7  # Първата числова променлива участва в аритметичните операции.
right_value = 3  # Втората числова променлива е вторият операнд.

sum_value = left_value + right_value  # Събираме двете стойности с оператора плюс.
logic_result = (left_value > right_value) and (left_value != right_value)  # Комбинираме логически оператори.

if left_value < right_value:  # Проверяваме първото условие.
    print("По-малка")  # Този ред би се изпълнил, ако първото условие е вярно.
elif left_value == right_value:  # Ако първото условие е невярно, проверяваме второто.
    print("Равна")  # Този ред би се изпълнил при равенство.
else:  # Ако нито едно от горните условия не е изпълнено, влизаме в else клона.
    print("По-голяма")  # Тук попадаме с текущите примерни стойности.
```

Циклите са показани и в трите практически форми:

```python
for index in range(3):  # Изпълняваме цикъл точно три пъти.
    print(index)  # Печатаме текущата стойност на брояча.

counter = 0  # Създаваме брояч за while цикъла.
while counter < 3:  # Повтаряме докато броячът е по-малък от три.
    print(counter)  # Печатаме текущата стойност на брояча.
    counter += 1  # Увеличаваме брояча, за да не останем в безкраен цикъл.

attempt = 0  # Създаваме брояч за демонстрацията.
while True:  # Влизаме в безкраен цикъл, който ще прекъснем с break.
    print(attempt)  # Печатаме текущата стойност, като това винаги се случва поне веднъж.
    attempt += 1  # Увеличаваме брояча след изпълнението на тялото.
    if attempt >= 2:  # Проверяваме условието за прекратяване след тялото на цикъла.
        break  # Напускаме цикъла, което прави конструкцията еквивалент на do-while.
```

Важни уточнения за начинаещи:

- `goto` няма в Python и в MicroPython
- `switch-case` няма в класическия MicroPython синтаксис на тези примери
- еквивалентите са `if/elif/else`, таблици, `break`, `continue`, `return`
- `do-while` се показва като `while True` + `break`

Файл:

- `examples/20_language_basics/03_operators_conditions_loops.py`

#### Резюме
- Главата въвежда темата „Оператори, условия и цикли“ чрез кратък изпълним MicroPython пример.
- Идеята е показана в чист синтаксис, без да се губи връзката с бъдещите хардуерни примери.
- Това е езиков строителен блок за по-сложните глави нататък.

#### Самопроверка
1. Каква е разликата между аритметичен оператор, оператор за сравнение и логически оператор?
2. Защо `while True` без условие за изход е опасен при учебен пример?
3. Как бихте използвали `if/elif/else` и цикъл в реален пример с LED, бутон или ADC?

#### Упражнения
1. Променете началните стойности на `left_value` и `right_value` така, че да минете и през трите клона на `if/elif/else`.
2. Напишете цикъл, който брои от `5` до `1`, а после отпечатва `START`.
3. Направете малък пример с `while True` и `break`, който спира след определен брой опити.

#### Ако не работи
- Симптом: програмата влиза в безкраен цикъл. Проверка: уверете се, че броячът наистина се променя и че имате условие за изход.
- Симптом: влиза грешният клон на условието. Проверка: отпечатайте междинните стойности и проверете последователно всяко условие.

#### Нататък
След тази глава преминете към „Enums в MicroPython“, за да видите как състояния и режими могат да станат по-четими чрез именувани стойности.

### Глава 5. Enums в MicroPython

Пример:

```python
from micropython import const  # Импортираме const, за да правим именувани константи като прост MicroPython еквивалент на enum.

STATE_OFF = const(0)  # Тази константа означава състояние "изключено".
STATE_ON = const(1)  # Тази константа означава състояние "включено".
STATE_BLINK = const(2)  # Тази константа означава състояние "мигане".
```

Това е правилният стил за тези уроци:

- именувани константи
- таблица за превод към четимо име

Файл:

- `examples/20_language_basics/04_enums.py`

#### Резюме
- Главата въвежда темата „Enums в MicroPython“ чрез кратък изпълним MicroPython пример.
- Идеята е показана в чист синтаксис, без да се губи връзката с бъдещите хардуерни примери.
- Това е езиков строителен блок за по-сложните глави нататък.

#### Самопроверка
1. Защо в MicroPython често използваме `const()` вместо пълен `enum` тип?
2. Каква полза има кодът, ако пишете `STATE_BLINK`, а не просто `2`?
3. В кой реален пример на борд бихте описали режимите като `STATE_OFF`, `STATE_ON`, `STATE_BLINK`?

#### Упражнения
1. Добавете още две състояния, например `STATE_ERROR` и `STATE_IDLE`, и направете таблица за печат на имената им.
2. Напишете `if/elif/else`, което реагира различно на всяко състояние.
3. Използвайте именувани състояния в минипример за LED: изключен, включен, мигащ.

#### Ако не работи
- Симптом: стойностите на състоянията са объркващи. Проверка: дръжте имената ясни и последователни, а числата стабилни.
- Симптом: при печат виждате числа, но не и смисъл. Проверка: добавете таблица или `dict`, който превежда състоянието до четимо име.

#### Нататък
След тази глава преминете към „Масиви и таблици“, за да започнете да съхранявате повече данни, а не само единични стойности.

### Глава 6. Масиви и таблици

Пример:

```python
from array import array  # Импортираме array, за да покажем по-компактен числов масив от list.

adc_samples = [1200, 1215, 1198, 1222]  # Това е обикновен list, който играе роля на масив от измервания.
led_table = [["LED1", "P204"], ["SW1", "P400"]]  # Това е вложен масив, тоест таблица от редове и колони.
pwm_duty_values = array("H", [0, 16384, 32768, 49152, 65535])  # Тук пазим 16-битови стойности в компактен масив.
raw_bytes = bytearray([0x10, 0x20, 0x30, 0x40])  # bytearray е удобен за сурови байтове и комуникационни буфери.
```

Тук има четири различни вида колекции:

- `list`
- вложен `list`, тоест масив от масиви
- `array`
- `bytearray`

Файл:

- `examples/20_language_basics/05_arrays.py`

#### Резюме
- Главата въвежда темата „Масиви и таблици“ чрез кратък изпълним MicroPython пример.
- Идеята е показана в чист синтаксис, без да се губи връзката с бъдещите хардуерни примери.
- Това е езиков строителен блок за по-сложните глави нататък.

#### Самопроверка
1. Кога бихте избрали `list`, кога `array`, и кога `bytearray`?
2. Какво печелите, ако съхранявате измервания в таблица, вместо в отделни променливи?
3. Защо `array("H", ...)` и `bytearray(...)` са особено полезни при периферии и буфери?

#### Упражнения
1. Добавете още няколко ADC проби и изчислете минимална, максимална и средна стойност.
2. Направете таблица с няколко бордови ресурса, например `LED1`, `SW1`, `P014`, `P015`, и ги отпечатайте ред по ред.
3. Създайте `bytearray` за суров пакет от 4 байта и променете два байта „на място“.

#### Ако не работи
- Симптом: получавате `IndexError`. Проверка: вижте дали индексът е в допустимите граници на масива или таблицата.
- Симптом: `array` не приема стойностите. Проверка: проверете type code-а, например `"H"` за 16-битови беззнакови стойности.

#### Нататък
След тази глава преминете към „Структури в MicroPython“, където вече не пазим само редици от стойности, а и по-смислени записи с имена на полета.

### Глава 7. Структури в MicroPython

Вместо `struct` от C, в MicroPython се ползват:

- `dict`
- `namedtuple`
- клас
- списък от такива обекти

Пример:

```python
from collections import namedtuple  # Импортираме namedtuple като лек и четим заместител на проста структура.

SensorRecord = namedtuple("SensorRecord", ("name", "pin", "unit"))  # Описваме поле по поле една проста структура.
light_sensor = {"name": "Light", "adc_pin": "P000", "limits": {"dark": 800, "bright": 3000}}  # dict е най-честият MicroPython заместител на структура.
devices = [{"name": "LED1", "pin": "P204", "kind": "output"}]  # Тук правим списък от структури, както в C бихме имали масив от struct.
```

Тук има и:

- структура от структури чрез вложен `dict`
- масив от структури чрез списък от `dict`

Файл:

- `examples/20_language_basics/06_structures.py`

#### Резюме
- Главата въвежда темата „Структури в MicroPython“ чрез кратък изпълним MicroPython пример.
- Идеята е показана в чист синтаксис, без да се губи връзката с бъдещите хардуерни примери.
- Това е езиков строителен блок за по-сложните глави нататък.

#### Самопроверка
1. Каква е разликата между `dict`, `namedtuple` и списък от `dict`?
2. Кога е по-удобно да пазите данните като „структура от структури“?
3. Как бихте описали с такава структура сензор, пин, прагове и режим на работа?

#### Упражнения
1. Добавете второ устройство в списъка `devices` и отпечатайте имената и пиновете и на двете.
2. Разширете `light_sensor` с още едно вложено поле, например `limits["alarm"]`.
3. Направете структура за бутон с име, pin, active level и debounce време.

#### Ако не работи
- Симптом: четете грешно поле. Проверка: различавайте достъп с ключ `record["name"]` от достъп по атрибут при `namedtuple`.
- Симптом: вложените данни се оказват по-дълбоки от очакваното. Проверка: отпечатайте цялата структура и проследете пътя до полето стъпка по стъпка.

#### Нататък
След тази глава преминете към „Указатели и еквивалентите им в MicroPython“, за да видите как споделяне и промяна на данни се случват без C синтаксис за указатели.

### Глава 8. Указатели и еквивалентите им в MicroPython

Тази глава е умишлено преведена към MicroPython, а не към C синтаксис.

Пример:

```python
adc_list = [100, 200, 300]  # Създаваме изменяем обект, който ще играе ролята на споделени данни.
alias_list = adc_list  # Тук не се копира list, а и двете имена сочат към един и същ обект.
alias_list[1] = 999  # Промяната през второто име се вижда и през първото име.

tx_buffer = bytearray([1, 2, 3, 4])  # bytearray е полезен за буфери, които се редактират "на място".
tx_view = memoryview(tx_buffer)  # memoryview дава прозорец към същите байтове без копиране.
tx_view[2] = 77  # Промяната през memoryview променя и оригиналния буфер.
```

Тук се показват:

- две имена към един и същ обект
- промяна на буфер „на място“
- `memoryview` като евтин прозорец към същите данни
- ниско ниво чрез `machine.mem8`, `mem16`, `mem32`

Файлове:

- `examples/20_language_basics/07_pointers_equivalents.py`
- `examples/28_machine_misc/02_machine_mem_and_bootloader.py`

#### Резюме
- Главата въвежда темата „Указатели и еквивалентите им в MicroPython“ чрез кратък изпълним MicroPython пример.
- Идеята е показана в чист синтаксис, без да се губи връзката с бъдещите хардуерни примери.
- Това е езиков строителен блок за по-сложните глави нататък.

#### Самопроверка
1. Защо `alias_list = adc_list` не прави копие, а второ име към същия обект?
2. Какво печелите от `memoryview`, ако не искате да копирате буфера?
3. Кога ниското ниво през `machine.mem32` е полезно и защо трябва да се ползва внимателно?

#### Упражнения
1. Направете второ име към един списък и докажете с печат, че промяна през едното име се вижда и през другото.
2. Създайте `bytearray`, после `memoryview` към него, и променете няколко байта през изгледа.
3. Обяснете с кратък експеримент разликата между „копие на данни“ и „втори изглед към същите данни“.

#### Ако не работи
- Симптом: променяте буфер, но оригиналът не се изменя. Проверка: вижте дали не сте направили копие, вместо изглед към същите данни.
- Симптом: ниско ниво през `mem32` дава странен резултат. Проверка: работете само ако знаете точния адрес и смисъла на регистъра.

#### Нататък
След тази глава преминете към „Функции, callbacks и таблици от функции“, където вече самото поведение се подава и избира като данни.

### Глава 9. Функции, callbacks и таблици от функции

Пример:

```python
def add_values(left_value, right_value):  # Дефинираме функция, която получава два входни параметъра.
    return left_value + right_value  # Връщаме резултата като изходна стойност.

def run_callback(callback, value):  # Дефинираме функция, която приема друга функция като параметър.
    callback(value)  # Извикваме подадената функция с подадената стойност.

def square(value):  # Това е първата функция за таблицата от функции.
    return value * value  # Връщаме квадрата на аргумента.

operation_table = [square]  # Това е списък от функции, тоест таблица от извикваеми обекти.
```

Тази глава покрива:

- входни параметри
- връщане на стойности
- multiple return values
- mutable параметри вместо „предаване с указател“
- функция като стойност
- списък от функции като таблица от „function pointers“

Файл:

- `examples/20_language_basics/08_functions.py`

#### Резюме
- Главата въвежда темата „Функции, callbacks и таблици от функции“ чрез кратък изпълним MicroPython пример.
- Идеята е показана в чист синтаксис, без да се губи връзката с бъдещите хардуерни примери.
- Това е езиков строителен блок за по-сложните глави нататък.

#### Самопроверка
1. Каква е разликата между това да извикате функция веднага и това да я подадете като callback?
2. Защо списъкът от функции може да се мисли като MicroPython еквивалент на таблица от function pointers?
3. Къде в бордов код бихте използвали callback или таблица от операции?

#### Упражнения
1. Добавете в `operation_table` още една функция, например кубиране или удвояване.
2. Напишете функция `run_selected(index, value)`, която избира функция от таблицата и я изпълнява.
3. Направете callback, който отпечатва стойност, а после заменете печата с друга callback функция.

#### Ако не работи
- Симптом: callback не се изпълнява. Проверка: вижте дали подавате самата функция, а не резултата от нейното предварително извикване.
- Симптом: таблицата от функции връща грешен резултат. Проверка: отпечатайте избрания индекс и името на функцията, която действително се вика.

#### Нататък
След тази глава преминете към „Цифров изход“, за да започнете да прилагате езиковите конструкции върху реален хардуерен ресурс.

## Част III: Цифрови и аналогови входове и изходи

### Глава 10. Цифров изход

Пример:

```python
from machine import Pin  # Импортираме класа Pin, за да използваме LED1 като цифров изход.
import time  # Импортираме time, за да правим видими паузи между смените на логическото ниво.

led = Pin("LED1", Pin.OUT, value=1)  # Настройваме LED1 като изход и го оставяме изгасен в началото.
led.value(0)  # Подаваме логическа нула на изхода.
time.sleep_ms(700)  # Изчакваме 700 ms, за да се види светването.
led.value(1)  # Подаваме логическа единица на изхода.
```

Тук се обясняват:

- логическа `0`
- логическа `1`
- защо при active-low LED те не означават директно „изключено“ и „включено“
- че допустимият ток и товароспособността се гледат по hardware manual и схемата на борда

Файлове:

- `examples/21_digital_io/01_digital_output.py`
- `examples/01_blink_async.py`

#### Резюме
- `Pin.OUT` прави пина управляван изход, а `value(0)` и `value(1)` задават логическото му ниво.
- При `LED1` логиката е active-low, затова `0` означава „светни“, а `1` означава „изгаси“.
- Това е първият момент в книгата, в който една единична стойност в кода води до пряк видим ефект на платката.

#### Самопроверка
1. Защо `LED1` светва при `value(0)`, а не при `value(1)`?
2. Каква е ролята на `Pin.OUT` в този пример?
3. Какво бихте променили първо, ако искате LED-ът да мига, вместо само да смени състояние веднъж?

#### Упражнения
1. Направете функция `blink_n(count, on_ms, off_ms)`, която мига LED1 зададен брой пъти.
2. Добавете `print("LED ON")` и `print("LED OFF")`, за да свържете логиката в кода с физическия ефект.
3. Направете две различни мигащи последователности: бавна и бърза.

#### Ако не работи
- Симптом: LED1 не реагира. Проверка: използвайте точно `Pin("LED1", Pin.OUT)` и започнете с прост пример без допълнителна логика.
- Симптом: поведението ви изглежда обърнато. Проверка: това е нормално за active-low LED и трябва да се мисли като част от борда, не като грешка в Python.
- Симптом: външен товар не се държи като LED. Проверка: GPIO пинът не е универсален източник на мощност; съобразете се с допустимия ток и товара.

#### Нататък
След тази глава преминете към „Цифров вход“, за да видите другата половина на GPIO света: не как подаваме ниво, а как го четем.

### Глава 11. Цифров вход

Пример:

```python
from machine import Pin  # Импортираме Pin, за да четем бутона SW1 като цифров вход.

button = Pin("SW1", Pin.IN, Pin.PULL_UP)  # Настройваме бутона като вход с вътрешен pull-up резистор.
print(button.value())  # Вземаме текущата сурова логическа стойност на входа.
```

Тук се обясняват:

- входно ниво
- активен нисък бутон
- вътрешен `PULL_UP`
- защо `PULL_DOWN` не е част от тези уроци за този порт

Файл:

- `examples/21_digital_io/02_digital_input.py`

#### Резюме
- `Pin.IN` прави пина вход, а `Pin.PULL_UP` му задава стабилно изходно състояние, когато бутонът не е натиснат.
- `SW1` е активен нисък бутон, затова натискането обикновено дава `0`, а ненатиснатото състояние дава `1`.
- Това е основата за всички следващи теми с бутони, debounce, polling и прекъсвания.

#### Самопроверка
1. Защо бутонът се конфигурира с `Pin.PULL_UP`, а не остава floating?
2. Какво означава `button.value() == 0` в този конкретен пример със `SW1`?
3. Защо само еднократно `print(button.value())` рядко е достатъчно в реално приложение с бутон?

#### Упражнения
1. Направете цикъл, който чете бутона периодично и печата `PRESSED` или `RELEASED`.
2. Свържете бутона с LED1 така, че натискането да включва или изключва светодиода.
3. Добавете прост брояч на натисканията и отпечатвайте общия брой след всяка промяна.

#### Ако не работи
- Симптом: входът се държи нестабилно. Проверка: уверете се, че ползвате `Pin.PULL_UP` и че не оставяте входа floating.
- Симптом: логиката е обърната. Проверка: помнете, че `SW1` е active-low и натискането дава `0`.
- Симптом: натискането се отчита по няколко пъти. Проверка: това е очаквано без debounce и ще бъде разгледано в следващите глави.

#### Нататък
След тази глава преминете към „Аналогов вход и ADC“, за да сравните двоичното четене на GPIO вход със света на непрекъснатите аналогови стойности.

### Глава 12. Аналогов вход и ADC

Пример:

```python
from machine import ADC  # Импортираме класа ADC, чрез който се четат аналоговите канали.

temperature_adc = ADC(ADC.CORE_TEMP)  # Създаваме ADC обект за вътрешния температурен канал.
vref_adc = ADC(ADC.CORE_VREF)  # Създаваме ADC обект за вътрешното опорно напрежение.

print(temperature_adc.read())  # Четем суровата ADC стойност на вътрешния температурен канал.
print(vref_adc.read())  # Четем суровата ADC стойност на вътрешното опорно напрежение.
```

Тук се учат:

- какво е ADC
- как напрежението става число
- разликата между сурово четене и `read_u16()`
- защо вътрешните канали са добър старт

Важно уточнение за `RA4M2`:

- Този MCU е с `ADC12`, тоест хардуерната резолюция тук е `12 bit`, а поддържаните режими са `12 / 10 / 8 bit`.
- В текущия порт може да се избира режим още при създаване на обекта: `ADC(pin_or_channel, bits=8)`, `ADC(..., bits=10)`, `ADC(..., bits=12)`.
- Настройката за `bits` е глобална за ADC периферията, а не отделна за всеки ADC обект.

Кратък пример:

```python
from machine import ADC  # Импортираме ADC за избор на резолюция при създаване на обекта.

adc_fast = ADC("P000", bits=8)  # Създаваме ADC обект с 8-битова резолюция за бързо четене.
adc_balanced = ADC("P001", bits=10)  # Създаваме ADC обект с 10-битова резолюция за баланс бързина/точност.
adc_precise = ADC("P002", bits=12)  # Създаваме ADC обект с 12-битова резолюция за максимална точност.

print(adc_fast.read(), adc_balanced.read(), adc_precise.read())  # Четем и трите канала и показваме резултатите.
```

Практическа бележка за времето на преобразуване при текущите тактове на `VK_RA4M2`:

- при минимално валидно семплиране и `ADCLK = 50 MHz` самото A/D преобразуване е приблизително `0.31 us` за `8 bit`, `0.35 us` за `10 bit`, `0.39 us` за `12 bit`
- при по-дълго семплиране, например около reset/default настройката на `ADSSTR`, стойностите са приблизително `0.41 us`, `0.45 us`, `0.49 us`
- това е време на хардуерния ADC, а не пълното време на извикването `ADC.read()` от Python
- ако искате да мерите целия път през MicroPython, използвайте `time.ticks_us()` около самото извикване
- за практическо измерване на пълното време на `ADC.read()` виж `examples/23_timing/07_adc_timing_measure.py`, където се мери последователно в `8 / 10 / 12 bit`

Наличните външни ADC пинове в този курс са:

`P000`, `P001`, `P002`, `P003`, `P004`, `P005`, `P006`, `P007`, `P008`, `P013`, `P014`, `P015`, `P500`

Файл:

- `examples/22_analog/01_analog_input_adc.py`
- `examples/23_timing/07_adc_timing_measure.py`

#### Резюме
- `ADC.read()` връща сурова числова стойност, която представя моментното аналогово ниво на избрания канал.
- Вътрешните канали `ADC.CORE_TEMP` и `ADC.CORE_VREF` са добър старт, защото не изискват външно окабеляване.
- На този порт `bits=8/10/12` се задава при създаване на обекта, но влияе на ADC периферията като цяло, а не само на един обект.
- Времето на самото A/D преобразуване и времето на `ADC.read()` от Python не са едно и също нещо.

#### Самопроверка
1. Каква е разликата между вътрешен ADC канал като `ADC.CORE_TEMP` и външен пин като `P000`?
2. Защо параметърът `bits=` трябва да се мисли като настройка на периферията, а не само на единичен обект?
3. Как бихте разграничили „бърз ADC хардуер“ от „по-бавно Python извикване на `ADC.read()`“?

#### Упражнения
1. Сравнете едно и също входно ниво при `bits=8`, `bits=10` и `bits=12` и запишете как се променя числовият диапазон.
2. Свържете потенциометър или прост делител към външен ADC пин и отпечатвайте стойностите с малко закъснение между четенията.
3. Измерете пълното време на `ADC.read()` с `time.ticks_us()` и го сравнете с ориентировъчните хардуерни времена в текста.

#### Ако не работи
- Симптом: стойностите са почти случайни. Проверка: външният вход може да е floating; дайте му реален източник на напрежение или стабилен делител.
- Симптом: получавате грешка или неочакван резултат от пина. Проверка: вижте дали ползвате валиден ADC пин от изброените в главата.
- Симптом: измереното време е много по-голямо от част от микросекунда. Проверка: това е нормално, защото мерите целия Python път, а не само вътрешното хардуерно преобразуване.

#### Нататък
След тази глава преминете към „PWM“, за да сравните два различни начина за работа с аналогово поведение: измерване чрез `ADC` и генериране чрез импулсен изход.

### Глава 13. PWM

Базов пример:

```python
from machine import Pin, PWM  # Импортираме Pin и PWM, за да генерираме PWM сигнал върху валиден PWM пин.

pwm = PWM(Pin("P107"), freq=1000, duty=50)  # Създаваме PWM обект с честота 1 kHz и duty 50 процента.
```

Разширен пример за общ GPT канал:

```python
pwm_a = PWM(Pin("P107"), freq=1000, duty=25)  # Създаваме PWM върху изход A с честота 1 kHz и duty 25 процента.
pwm_b = PWM(Pin("P106"), freq=1000, duty=75)  # Създаваме PWM върху изход B със същата честота, но с duty 75 процента.
pwm_a.freq(2000)  # Променяме честотата от страна на изход A, което ще промени и канала B, защото са върху един GPT канал.
```

Тук се вижда, че:

- duty може да е различен
- честотата се споделя от двата изхода на общия GPT канал
- на този порт PWM е върху GPT периферията и общият канал налага обща `freq()` за A/B изходите
- проверката на реалната форма, фронтове и jitter става с осцилоскоп или логически анализатор, а не само по print от Python

Файлове:

- `examples/22_analog/02_pwm_output.py`
- `examples/03_pwm_ab_same_channel.py`

#### Резюме
- `PWM` в този порт стъпва върху `GPT`, затова честотата е свойство на канала, а не само на отделния пин.
- Двата изхода `A/B` на един и същ `GPT` канал могат да имат различен `duty`, но споделят обща `freq()`.
- `PWM` е удобен за яркост, управление и сигнални експерименти, но не е същото като истински аналогов `DAC` изход.
- Ако ви интересуват форма, фронтове и jitter, гледайте сигнала с осцилоскоп или логически анализатор, не само с `print()`.

#### Самопроверка
1. Защо промяната на `pwm_a.freq()` влияе и на `pwm_b`, ако двата пина са върху общ `GPT` канал?
2. Каква е практическата разлика между `freq` и `duty`?
3. Защо `print()` не е достатъчен, ако искате да проверите точната форма на PWM сигнала?

#### Упражнения
1. Променете `duty` през няколко стойности и наблюдавайте как се изменя яркостта или усредненото ниво на товара.
2. Пуснете двата изхода от общия `GPT` канал и проверете как общата честота се пази, докато `duty` остава различен.
3. Сравнете `500 Hz`, `1000 Hz` и `2000 Hz` и запишете какво се вижда на товар или измервателен инструмент.

#### Ако не работи
- Симптом: няма PWM ефект на пина. Проверка: вижте дали пинът е валиден PWM изход за този борд и дали не е в конфликт с друг периферен режим.
- Симптом: вторият канал се „променя сам“. Проверка: ако е върху същия `GPT`, това е очаквано за `freq()`, защото честотата е общ ресурс.
- Симптом: резултатът изглежда шумен или нестабилен. Проверка: различавайте електрическия шум и товара от реалния PWM timing; проверете и с измервателен инструмент.

#### Нататък
След тази глава преминете към „Закъснения, хардуерни таймери и `Timer(-1)`“, защото именно таймерите стоят под много от сигналните и PWM базирани поведения.

## Част IV: Таймери, прекъсвания и време

### Глава 14. Закъснения, хардуерни таймери и `Timer(-1)`

Блокиращо закъснение:

```python
import time  # Импортираме time за блокиращото закъснение.
time.sleep_ms(300)  # Това закъснение блокира изпълнението на останалия код.
```

Хардуерен таймер:

```python
from machine import Timer  # Импортираме Timer за хардуерния таймер.

timer = Timer(1)  # Използваме един от наличните хардуерни machine.Timer канали от Timer(1) до Timer(6).
timer.init(freq=4, callback=timer_callback, hard=False)  # Стартираме Timer(1) на 4 Hz с callback извън hard IRQ контекст.
```

Софтуерен таймер:

```python
from machine import Pin, Timer  # Импортираме Pin и Timer за демонстрацията на софтуерния таймер.
import time  # Импортираме time, за да оставим таймера да работи кратко време.

led = Pin("LED1", Pin.OUT)  # Вземаме LED1 като видим индикатор за сработване на callback-а.
soft_timer = Timer(-1)  # Създаваме софтуерен таймер чрез идентификатор -1.
soft_state = {"count": 0}  # Пазим броя сработвания в mutable структура.
started = False  # С този флаг ще следим дали таймерът е стартирал успешно.

def soft_timer_callback(timer_object):  # Това е функцията, която ще се извиква при всяко сработване на софтуерния таймер.
    soft_state["count"] = soft_state["count"] + 1  # Увеличаваме брояча при всяко събитие.
    led.value(0 if led.value() else 1)  # Превключваме LED, за да виждаме че callback-ът се изпълнява.

if not started:  # Ако още не сме стартирали таймера, пробваме втория стил с честота.
    try:  # Влизаме в защитен блок за втория опит.
        soft_timer.init(freq=4, callback=soft_timer_callback, hard=False)  # Пробваме инициализация с честота 4 Hz.
        started = True  # Отбелязваме, че вторият стил е успял.
        print("Timer(-1) тръгна с freq API.")  # Печатаме кой резервен стил е сработил.
    except Exception as second_error:  # Ако и вторият стил не тръгне, само информираме потребителя.
        print("freq API също не тръгна:", second_error)  # Показваме причината и за втория неуспех.
```

Тук трябва да остане ясно:

- `Timer(1)` до `Timer(6)` са хардуерни
- `Timer(-1)` е софтуерен
- `asyncio` е още една неблокираща алтернатива

Практическа бележка за `time.ticks_us()`:

- ползвайте `time.ticks_us()` и `time.ticks_diff()` за относителни интервали в микросекунди
- в този порт `time.ticks_us()` идва от хардуерно поддържан `mp_hal_ticks_us()`, а не от измислен Python брояч
- това е много полезно за сравнение на latency и `ADC.read()` време, но не е заместител на осцилоскоп при хардуерна прецизност

Файлове:

- `examples/23_timing/01_delays_and_timers.py`
- `examples/23_timing/04_software_timer_minus1.py`
- `examples/23_timing/05_timer_compare_hw_sw.py`
- `examples/23_timing/07_adc_timing_measure.py`
- `examples/23_timing/13_hardware_timer_periodic_oneshot.py`
- `examples/23_timing/14_hardware_timer_output_compare.py`
- `examples/23_timing/15_hardware_timer_input_capture.py`
- `examples/23_timing/16_hardware_timer_event_count.py`

#### Подглава 14.1. Хардуерни таймери

След базовата идея за `Timer(1)` и `Timer(-1)` е важно да се види какво реално може хардуерният таймер на този порт.

В текущия firmware за `VK_RA4M2`:

- `Timer(1)` до `Timer(6)` са хардуерни AGT таймери
- `Timer(-1)` е софтуерен таймер върху системния tick
- хардуерните таймери поддържат `Timer.PERIODIC` и `Timer.ONE_SHOT`
- налични са `freq()`, `period()` и `counter()`
- налични са `output compare`, `input capture` и `event count`
- при `event count` няма channel callback; ако искате callback на всеки `N` импулса, ползвате базовия `Timer.callback()` и задавате `period=N`

Базов пример за periodic и one-shot:

```python
from machine import Timer  # Импортираме Timer, за да използваме хардуерните таймери.

periodic_timer = Timer(1)  # Вземаме Timer(1) за периодичен режим.
oneshot_timer = Timer(2)  # Вземаме Timer(2) за еднократен режим.

periodic_timer.init(mode=Timer.PERIODIC, freq=2, callback=periodic_callback, hard=False)  # Пускаме периодичен таймер на 2 Hz.
print(periodic_timer.period())  # Показваме периода в сурови AGT counts.
print(periodic_timer.counter())  # Показваме текущия суров counter.

oneshot_timer.init(mode=Timer.ONE_SHOT, freq=2, callback=oneshot_callback, hard=False)  # Пускаме еднократен таймер със същия базов период.
```

Това се вижда подробно в:

- `examples/23_timing/13_hardware_timer_periodic_oneshot.py`

Пример за output compare callback:

```python
from machine import Timer  # Импортираме Timer, защото output compare работи през хардуерния таймер.

timer = Timer(1)  # Използваме Timer(1) като базов таймер за compare събитията.
timer.init(mode=Timer.PERIODIC, freq=10, callback=cycle_callback, hard=False)  # Пускаме бавен периодичен таймер на 10 Hz.

period_counts = timer.period()  # Вземаме периода в сурови counts.
channel_a = timer.channel(1, mode=Timer.OC, compare=period_counts // 4, callback=compare_a_callback)  # Настройваме compare A на 1/4 от периода.
channel_b = timer.channel(2, mode=Timer.OC, compare=(period_counts * 3) // 4, callback=compare_b_callback)  # Настройваме compare B на 3/4 от периода.
```

Това се вижда подробно в:

- `examples/23_timing/14_hardware_timer_output_compare.py`

Пример за input capture:

```python
from machine import Pin, PWM, Timer  # Импортираме Pin, PWM и Timer за генерация и измерване на тестов сигнал.

signal_pwm = PWM(Pin("P107"), freq=1000, duty=50)  # Генерираме тестов квадратен сигнал 1 kHz на P107.
timer = Timer(1)  # Използваме Timer(1) за input capture измерването.

timer.init(mode=Timer.PERIODIC, freq=1_000_000, hard=False)  # Пускаме Timer(1) на 1 MHz, за да е 1 count приблизително 1 us.
period_channel = timer.channel(0, mode=Timer.IC, pin=Pin("P100"), measure=Timer.IC_PERIOD, edge=Timer.RISING)  # Мерим периода на сигнала на P100.
```

Това се вижда подробно в:

- `examples/23_timing/15_hardware_timer_input_capture.py`

Пример за event count и callback на всеки `N` импулса:

```python
from machine import Pin, PWM, Timer  # Импортираме Pin, PWM и Timer за броене на входни импулси.

signal_pwm = PWM(Pin("P107"), freq=200, duty=50)  # Генерираме входен тестов сигнал 200 Hz на P107.
timer = Timer(1)  # Използваме Timer(1) като хардуерен брояч на събития.

timer.init(mode=Timer.PERIODIC, period=20, callback=every_n_events_callback, hard=False)  # Искаме callback на всеки 20 импулса.
counter_channel = timer.channel(0, mode=Timer.IC, pin=Pin("P100"), measure=Timer.IC_EVENT_COUNT, edge=Timer.RISING)  # Пускаме channel(0) в event count режим.
print(counter_channel.capture())  # Четем частичния брой импулси в текущия прозорец.
```

Тук е важната идея:

- channel callback за `Timer.IC_EVENT_COUNT` няма
- `capture()` се ползва за polling на частичния брой
- базовият `Timer.callback()` се ползва когато искате реакция на всеки `N` входни импулса

Това се вижда подробно в:

- `examples/23_timing/16_hardware_timer_event_count.py`

#### Подглава 14.2. Практически checklist за AGT тестове

Тази подглава е кратък runtime checklist за проверка на новия хардуерен timer слой директно на борда.

Ако искаш отделен файл за работа до платката, използвай и:

- `examples/AGT_TEST_CHECKLIST.md`

Преди старта:

- качи актуалния firmware на борда
- качи актуалната директория `examples/` на `/flash/examples`
- отвори REPL, за да виждаш `print()` резултатите
- ако ще гледаш физически изходи, подготви логически анализатор или осцилоскоп

Бърз ред на тестовете:

1. Стартирай `examples/23_timing/13_hardware_timer_periodic_oneshot.py`
2. Стартирай `examples/23_timing/14_hardware_timer_output_compare.py`
3. Свържи `P107 -> P100` с джъмпер
4. Стартирай `examples/23_timing/15_hardware_timer_input_capture.py`
5. Остави същия джъмпер `P107 -> P100`
6. Стартирай `examples/23_timing/16_hardware_timer_event_count.py`

Тест 1: periodic и one-shot

- `Timer(1)` трябва да сработва многократно
- `Timer(2)` трябва да сработи само веднъж
- `Timer(1).counter()` трябва да се променя, докато таймерът работи
- проблем е ако one-shot не дойде или ако дойде повече от веднъж

Тест 2: output compare callback

- `Cycle-end callback count` трябва да расте
- `Compare A callback count` трябва да расте
- `Compare B callback count` трябва да расте
- проблем е ако cycle callback работи, а compare callback-ите стоят на `0`

Опционален хардуерен тест за output compare:

- за `Timer(1)` можеш да добавиш `pin=Pin("P500")` на `channel(1)`
- за `Timer(1)` можеш да добавиш `pin=Pin("P501")` на `channel(2)`
- на осцилоскоп очаквай събития около `1/4` и `3/4` от периода

Тест 3: input capture

- нужен е джъмпер `P107 -> P100`
- при PWM `1 kHz / 50% duty` очаквай `Измерен период` около `1000`
- при същия сигнал очаквай `Измерена high ширина` около `500`
- проблем е ако стойностите са `0`, силно шумни или high ширината е по-голяма от периода

Тест 4: event count

- нужен е джъмпер `P107 -> P100`
- входният PWM е `200 Hz`
- прозорецът е `20` импулса
- за около `1.1 s` очаквай приблизително `10` или `11` завършени прозореца
- `Частичен брой в текущия прозорец` трябва да е между `0` и `19`
- `Приблизителен общ брой импулси` трябва да е близо до `220`

Тук е важно:

- при `Timer.IC_EVENT_COUNT` няма channel callback
- `capture()` се ползва за polling на частичния брой
- базовият `Timer.callback()` се ползва когато искаш реакция на всеки `N` импулса

Ако нещо не работи:

- провери първо джъмпера `P107 -> P100`
- провери дали не е останал стар обект `PWM` или `Timer` от предишен тест
- извикай `deinit()` на използваните обекти и повтори теста
- ако `periodic` работи, но `compare` не работи, проблемът е в compare IRQ path-а
- ако `compare` работи, но `capture` и `event count` не работят, проблемът е в AGT input pin или input mode path-а

Минимален smoke test:

1. `examples/23_timing/13_hardware_timer_periodic_oneshot.py`
2. `examples/23_timing/15_hardware_timer_input_capture.py` с джъмпер `P107 -> P100`
3. `examples/23_timing/16_hardware_timer_event_count.py` със същия джъмпер

Ако тези три минат, базовият AGT слой е в добро състояние.

#### Резюме
- `time.sleep_ms()` е най-простият инструмент, но блокира изпълнението на всичко останало.
- Хардуерният `Timer(n)` е подходящ, когато искате периодично действие да върви независимо от основния код.
- `Timer(-1)` е полезен като софтуерен таймер, но трябва да се мисли различно от истински хардуерен таймер.
- Тази глава полага основата за следващите теми с прекъсвания, debounce и събитийна логика.

#### Самопроверка
1. Каква е практическата разлика между `time.sleep_ms()`, `Timer(n)` и `Timer(-1)`?
2. Кога е по-подходящо да ползвате хардуерен таймер вместо цикъл със `sleep_ms()`?
3. Защо дълъг блокиращ код в callback или основния цикъл разваля времевото поведение?

#### Упражнения
1. Реализирайте едно и също мигане на LED1 веднъж със `sleep_ms()`, веднъж с хардуерен таймер и сравнете поведението.
2. Променете периода на таймера от `4 Hz` на `2 Hz` и `8 Hz` и запишете какво се променя.
3. Използвайте `examples/23_timing/05_timer_compare_hw_sw.py` и сравнете усещането за точност между `Timer(1)` и `Timer(-1)`.

#### Ако не работи
- Симптом: таймерът сработва с грешна честота. Проверка: вижте дали мислите в `freq` или в милисекунди и не смесвате единиците.
- Симптом: всичко „замръзва“ по време на демонстрацията. Проверка: вероятно сте оставили твърде дълъг блокиращ код в основния поток или в callback-а.

#### Нататък
След тази глава преминете към „Прекъсвания“, за да надградите таймерното мислене с реакция на външни събития.

### Глава 15. Прекъсвания

Пример:

```python
from machine import Pin, disable_irq, enable_irq  # Импортираме Pin, disable_irq и enable_irq за демонстрацията.

button = Pin("SW1", Pin.IN, Pin.PULL_UP)  # Настройваме SW1 като вход с pull-up.

def button_handler(pin_object):  # Това е функцията, която ще се вика при прекъсване от бутона.
    print("IRQ")  # Печатаме текст при всяко прекъсване.

button.irq(handler=button_handler, trigger=Pin.IRQ_FALLING, hard=False)  # Регистрираме обработчик за падащ фронт на SW1.
```

Безопасно четене на споделени данни:

```python
irq_state = disable_irq()  # Временно забраняваме прекъсванията, за да вземем консистентно копие на брояча.
snapshot = irq_state_data["count"]  # Копираме брояча, докато прекъсванията са спрени.
enable_irq(irq_state)  # Възстановяваме предишното състояние на прекъсванията.
```

Това е преходът към реално събитийно програмиране.

Практическа бележка за измерване на latency:

- може да се направи полезна ориентировъчна оценка чрез два свързани GPIO пина и `time.ticks_us()`
- тази стойност включва GPIO пътя, IRQ доставката, soft callback scheduling и Python callback overhead
- затова тя не е равна на „чистата“ силициева interrupt latency от datasheet или осцилоскоп
- примерът `examples/23_timing/08_interrupt_latency_measure.py` иска jumper между изходен и входен пин

Файлове:

- `examples/23_timing/02_interrupts.py`
- `examples/23_timing/08_interrupt_latency_measure.py`
- `examples/28_machine_misc/02_machine_mem_and_bootloader.py`

#### Резюме
- Прекъсванията позволяват бордът да реагира веднага на събитие, вместо да чака polling цикълът да стигне до него.
- Краткият IRQ handler и безопасното споделяне на данни са по-важни от това „да направите всичко вътре в прекъсването“.
- Измерването на latency е полезно, но трябва да се разбира като практична оценка на целия път, не като чиста силициева стойност.

#### Самопроверка
1. Каква е разликата между polling и IRQ реакция при бутон?
2. Защо е добра практика IRQ handler-ът да е кратък и прост?
3. Кога и защо бихте използвали `disable_irq()` и `enable_irq()` около споделени данни?

#### Упражнения
1. Направете IRQ обработчик, който не печата директно, а само брои събитията в споделен брояч.
2. Сравнете визуално или по лог polling четене и IRQ четене на един и същ бутон.
3. Пуснете latency примера с jumper и запишете ориентировъчната стойност, която виждате на вашия setup.

#### Ако не работи
- Симптом: получавате твърде много IRQ събития от едно натискане. Проверка: това е нормално без debounce и ще се реши в следващата глава.
- Симптом: обработчикът се държи странно или забавя системата. Проверка: намалете работата вътре в IRQ и изнесете тежките действия извън него.

#### Нататък
След тази глава преминете към „Дебаунс на бутон“, за да видите как реалният механичен вход се превръща в надеждно събитие.

### Глава 16. Дебаунс на бутон

Пример с polling:

```python
if current_value != last_stable_value:  # Проверяваме дали е настъпила промяна спрямо последното стабилно състояние.
    now_ms = time.ticks_ms()  # Вземаме текущото време само при промяна.
    if time.ticks_diff(now_ms, last_change_ms) >= 40:  # Приемаме промяната за валидна само ако е минало поне 40 ms.
        last_change_ms = now_ms  # Обновяваме момента на последната приета промяна.
        last_stable_value = current_value  # Приемаме новата стойност за стабилна.
```

Пример с `uasyncio`:

```python
if stable_value == 0 and stable_count == 4:  # Ако видим четири поредни проби с 0, приемаме валидно натискане.
    async_presses += 1  # Увеличаваме броя на валидните асинхронни натискания.
```

Тук има два подхода:

- времеви прозорец в polling цикъл
- последователни стабилни проби в async задача

Файл:

- `examples/23_timing/03_button_debounce.py`

#### Резюме
- Механичният бутон не дава чист единичен преход, а кратък шумен пакет от промени.
- Debounce логиката превежда суровото ниво към стабилно натискане и отпускане.
- Времевият прозорец и броят стабилни проби са двата основни инструмента за овладяване на този шум.

#### Самопроверка
1. Защо един реален бутон има нужда от debounce, въпреки че логиката му изглежда двоична?
2. Каква е разликата между debounce чрез времеви прозорец и debounce чрез поредица стабилни проби?
3. Какво ще стане, ако debounce прозорецът е твърде кратък или твърде дълъг?

#### Упражнения
1. Променете debounce времето от `40 ms` на `10 ms` и `80 ms` и сравнете стабилността.
2. Добавете печат само при валидно натискане, а не при всяка сурова промяна.
3. Сравнете polling варианта и `uasyncio` варианта върху един и същ бутон.

#### Ако не работи
- Симптом: едно натискане се брои по няколко пъти. Проверка: debounce прагът е твърде нисък или пробовземането е твърде грубо.
- Симптом: бутонът започва да реагира мудно. Проверка: debounce прозорецът вероятно е прекалено голям.

#### Нататък
След тази глава преминете към „Четене на бутон: `polling`, IRQ, `Timer(-1)`, debounce и събития“, за да сравните няколко практични подхода към един и същ входен проблем.

### Глава 17. Четене на бутон: `polling`, IRQ, `Timer(-1)`, debounce и събития

Тази глава събира на едно място реалните начини за работа с бутон върху `VK_RA4M2`.

Суровото четене на `SW1` е само началото.

Практическият въпрос е как да преведем шумния active-low вход към стабилни събития като:

- `PRESS`
- `RELEASE`
- `CLICK`
- `LONG_PRESS`
- `HOLD_START`
- `HOLD_REPEAT`

Първата идея е проста: `button.value()` ни дава ниво, но приложението почти винаги иска събития.

Пример с `polling` и преходи между стабилни състояния:

```python
raw_value = button.value()  # Четем суровата моментна стойност на бутона.
if raw_value != last_raw_value:  # Ако видим ново сурово ниво, започваме нов debounce прозорец.
    last_raw_value = raw_value  # Запомняме новата сурова стойност.
    raw_change_ms = now_ms  # Запомняме кога е започнала промяната.
if raw_value != stable_value and time.ticks_diff(now_ms, raw_change_ms) >= DEBOUNCE_MS:  # Ако новото ниво е стабилно достатъчно дълго, приемаме ново логическо състояние.
    stable_value = raw_value  # Обновяваме стабилното състояние на бутона.
    if stable_value == 0:  # Ако стабилното състояние е 0, имаме валидно натискане.
        print("EVENT: PRESS")  # Печатаме събитие за натискане.
    else:  # Ако стабилното състояние е 1, имаме валидно отпускане.
        print("EVENT: RELEASE")  # Печатаме събитие за отпускане.
```

Тук вече имаме важна промяна в начина на мислене:

- не гледаме „ниво в момента“
- гледаме „последно стабилно състояние“
- генерираме събитие само при стабилен преход

Ако искаме по-бърза реакция от чистото `polling`, ползваме прекъсване, но не правим тежката логика вътре в IRQ обработчика.

Пример с `IRQ_FALLING | IRQ_RISING` и отложена обработка:

```python
def button_irq_handler(pin_object):  # Това е краткият IRQ обработчик за суровите фронтове на бутона.
    irq_state_data["pending"] = 1  # Маркираме, че главният цикъл трябва да довърши логическата обработка.
    irq_state_data["deadline_ms"] = time.ticks_add(time.ticks_ms(), DEBOUNCE_MS)  # Отлагаме окончателното решение след debounce интервала.

if pending and time.ticks_diff(now_ms, deadline_ms) >= 0:  # Ако има чакаща работа и debounce прозорецът е изтекъл, вземаме стабилно решение.
    stable_candidate = button.value()  # Четем реалното ниво след краткото изчакване.
    if stable_candidate != previous_stable_value:  # Ако стабилното ниво реално е ново, генерираме логическо събитие.
        print("EVENT: PRESS" if stable_candidate == 0 else "EVENT: RELEASE")  # Печатаме кое логическо събитие е настъпило.
```

Това е по-добрият стил за бутон по прекъсване:

- IRQ само събужда логиката
- debounce се решава извън ISR
- събитието пак е стабилен `PRESS` или `RELEASE`, а не суров фронт

Има и по-ниско ниво на работа, когато искаме самото прекъсване да е максимално кратко и да не влиза в Python callback пътя.

Тогава ползваме `Pin.irq(..., fast=True)` и `@micropython.asm_thumb`, а извън IRQ само консумираме кратки събития от shared буфер.

Това е подходящо, когато:

- искаме минимална работа в IRQ
- искаме една малка shared опашка в RAM
- искаме печатът и по-тежката логика да останат извън прекъсването

Пример с два бутона `P301` и `P302`, `fast ASM IRQ`, shared `bytearray` ring buffer и debounce чрез lock + стабилно отпускане:

```python
from array import array  # Импортираме array, за да държим shared 32-bit стойности в RAM.
from machine import Pin  # Импортираме Pin, за да вържем бутоните към external IRQ линиите.
from uctypes import addressof  # Импортираме addressof, за да дадем на ASM кода адресите на shared буферите.
import machine  # Импортираме machine, за да ползваме disable_irq/enable_irq около consumer логиката.
import micropython  # Импортираме micropython, защото fast IRQ handler-ите са @micropython.asm_thumb.
import time  # Импортираме time, за да направим release debounce извън IRQ.

events = bytearray(16)  # Това е ring buffer-ът за кратките събития от прекъсването.
event_values = array("i", [0] * 16)  # Тук пазим snapshot на стойността за всяко събитие.
state = array("i", [0, 0, 0, 0, 0, 0])  # Държим wr_idx, rd_idx, value, lost_events, plus_lock и minus_lock.

queue_addr = addressof(events)  # Взимаме адреса на ring buffer-а за директен byte достъп от ASM.
values_addr = addressof(event_values)  # Взимаме адреса на snapshot буфера за директен word достъп от ASM.
state_addr = addressof(state)  # Взимаме адреса на state блока за директен word достъп от ASM.

exec(build_button_isr_source("plus_isr", +1, 1), globals(), globals())  # Генерираме fast ASM ISR за бутона "+".
exec(build_button_isr_source("minus_isr", -1, 2), globals(), globals())  # Генерираме fast ASM ISR за бутона "-".

plus_button = Pin("P301", Pin.IN, Pin.PULL_UP)  # Настройваме P301 като вход с pull-up за бутона "+".
minus_button = Pin("P302", Pin.IN, Pin.PULL_UP)  # Настройваме P302 като вход с pull-up за бутона "-".

plus_button.irq(trigger=Pin.IRQ_FALLING, handler=plus_isr, fast=True)  # Връзваме fast IRQ на падащ фронт за бутона "+".
minus_button.irq(trigger=Pin.IRQ_FALLING, handler=minus_isr, fast=True)  # Връзваме fast IRQ на падащ фронт за бутона "-".

while True:  # Главният цикъл остава извън IRQ и само довършва debounce и печата събитията.
    handle_release_debounce()  # Re-arm-ваме бутона едва след стабилно отпускане.
    irq_state = machine.disable_irq()  # Кратко спираме IRQ, за да вземем консистентен snapshot на опашката.
    event_code, event_value, lost_events = pop_event()  # Вземаме едно събитие и snapshot стойността му от shared буфера.
    machine.enable_irq(irq_state)  # Връщаме IRQ веднага след кратката критична секция.
    if event_code == 1:  # Ако кодът е 1, печатаме събитие за бутона "+".
        print("PLUS  value =", event_value)  # Показваме snapshot стойността за натискане на бутона "+".
    elif event_code == 2:  # Ако кодът е 2, печатаме събитие за бутона "-".
        print("MINUS value =", event_value)  # Показваме snapshot стойността за натискане на бутона "-".
    if lost_events:  # Ако ring buffer-ът е бил пълен, показваме, че има изпуснати събития.
        print("Queue overflow, lost events =", lost_events)  # Печатаме диагностиката за overflow извън IRQ.
    time.sleep_ms(2)  # Оставяме кратък sleep, за да е четим изходът и да работи release debounce логиката.
```

Тук ключовата идея е:

- fast IRQ path не печата и не пипа Python обекти
- ISR прави само три неща: lock, update на стойността, enqueue на кратко събитие
- debounce не е оставен на механичния бутон, а се довършва извън IRQ чрез lock + стабилно отпускане
- печатът става по snapshot стойността за самото събитие, не по по-късна глобална стойност

Пълният пример е във файла по-долу. Там helper-ът `build_button_isr_source(...)` генерира реалния `@micropython.asm_thumb` код и подава към него адресите на shared буферите.

Файл:

- `examples/23_timing/17_fast_irq_two_buttons_queue.py`

Третият подход е `Timer(-1)` като периодичен sampler.

Той е полезен, когато искаме еднакъв ритъм на пробовземане и по-богати събития за задържане.

Пример със софтуерен `Timer(-1)` и hold логика:

```python
soft_timer.init(period=SAMPLE_MS, mode=Timer.PERIODIC, callback=button_sample_callback)  # Стартираме Timer(-1) като периодичен sampler на бутона.
if timer_state["history"] == 0x00 and timer_state["stable_value"] != 0:  # Ако последните осем проби са 0, приемаме стабилно натискане.
    timer_state["event_code"] = EVENT_PRESS  # Записваме PRESS събитие за основния цикъл.
elif timer_state["stable_value"] == 0 and not timer_state["hold_started"] and time.ticks_diff(now_ms, timer_state["press_start_ms"]) >= LONG_PRESS_MS:  # Ако бутонът е натиснат достатъчно дълго, излъчваме начало на задържане.
    timer_state["event_code"] = EVENT_HOLD_START  # Записваме HOLD_START събитие.
elif timer_state["stable_value"] == 0 and timer_state["hold_started"] and time.ticks_diff(now_ms, timer_state["next_repeat_ms"]) >= 0:  # Ако вече сме в задържане и е дошло време за повторение, излъчваме repeat.
    timer_state["event_code"] = EVENT_HOLD_REPEAT  # Записваме HOLD_REPEAT събитие.
```

Тук се появяват вече различни класове събития:

- `PRESS` при стабилен преход `1 -> 0`
- `RELEASE` при стабилен преход `0 -> 1`
- `CLICK` при кратко натискане и отпускане
- `LONG_PRESS` при достигане на праг по време на задържане
- `HOLD_START` при началото на продължително задържане
- `HOLD_REPEAT` при периодично повтаряне, докато бутонът остава натиснат

Четвъртият подход е debounce чрез битово изместване.

Той е компактен и много удобен за периодичен sampler, защото свива последните `N` проби в една история.

Пример с shift-register debounce:

```python
history = ((history << 1) | button.value()) & 0xFF  # Преместваме историята наляво и добавяме новата проба като най-млад бит.
if history == 0x00 and stable_value != 0:  # Ако последните осем проби са натиснати, приемаме стабилно PRESS събитие.
    print("EVENT: PRESS")  # Печатаме събитие за натискане.
elif history == 0xFF and stable_value != 1:  # Ако последните осем проби са отпуснати, приемаме стабилно RELEASE събитие.
    print("EVENT: RELEASE")  # Печатаме събитие за отпускане.
```

Тук смисълът на `history` е директен:

- `0x00` означава осем поредни проби с натиснат бутон
- `0xFF` означава осем поредни проби с отпуснат бутон
- всички междинни стойности означават „още не е стабилно“

Практически избор на подход:

- ако искате най-простия старт, започнете с `polling`
- ако искате бързо събуждане на логиката, използвайте IRQ с отложена обработка
- ако искате равномерно пробовземане и hold/repeat семантика, използвайте `Timer(-1)`
- ако искате компактен debounce филтър, използвайте битово изместване

Файлове:

- `examples/23_timing/09_button_polling_events.py`
- `examples/23_timing/10_button_irq_deferred.py`
- `examples/23_timing/17_fast_irq_two_buttons_queue.py`
- `examples/23_timing/11_button_soft_timer_hold.py`
- `examples/23_timing/12_button_shift_debounce.py`
- `examples/23_timing/02_interrupts.py`
- `examples/23_timing/03_button_debounce.py`
- `examples/23_timing/04_software_timer_minus1.py`

#### Резюме
- Главата събира четенето на бутон като ниво, като стабилен преход и като събитийна система.
- Показани са `polling`, IRQ с отложена обработка, `fast ASM IRQ` със shared queue, `Timer(-1)` sampler и debounce чрез битово изместване.
- Основната идея е, че приложението почти винаги иска `PRESS`, `RELEASE` и `HOLD` събития, а не суров шумен вход.

#### Самопроверка
1. Каква е разликата между суров фронт от IRQ и стабилно логическо събитие `PRESS`?
2. Кога `Timer(-1)` е по-подходящ от чист `polling` цикъл за бутон?
3. Защо при `fast ASM IRQ` е по-добре да използваме shared ring buffer, вместо да печатаме директно от ISR?
4. Какво означават `0x00` и `0xFF` при debounce чрез битово изместване?

#### Упражнения
1. Разширете `09_button_polling_events.py`, така че след `LONG_PRESS` да не печата и `CLICK` при отпускане.
2. Добавете `CLICK` и към `10_button_irq_deferred.py`, като използвате продължителността между `PRESS` и `RELEASE`.
3. Пуснете `17_fast_irq_two_buttons_queue.py` с два бутона на `P301` и `P302` и измерете колко реда се печатат при едно натискане.
4. Променете размера на debounce историята в `12_button_shift_debounce.py` от 8 на 4 проби и сравнете устойчивостта.

#### Ако не работи
- Симптом: получавате много `PRESS` и `RELEASE` събития от едно натискане. Проверка: debounce прозорецът или пробовземането са твърде къси.
- Симптом: `LONG_PRESS` или `HOLD_REPEAT` не идват. Проверка: вижте дали държите бутона достатъчно дълго и дали прагът за задържане не е твърде голям.

#### Нататък
След тази глава преминете към „RTC“, за да добавите календарно и часово време към таймерното мислене.

### Глава 18. RTC

Пример:

```python
from machine import RTC  # Импортираме RTC, за да работим с часовника в реално време.

rtc = RTC()  # Създаваме обект за достъп до вградения RTC модул.
print(rtc.info())  # Печатаме диагностичната информация за стартиране на RTC.
print(rtc.calibration())  # Печатаме текущата калибрационна стойност.
print(rtc.datetime())  # Четем текущата дата и час като осемелементен tuple.
```

Тази глава покрива:

- диагностика на RTC
- current datetime tuple
- безопасно задаване на време зад флаг

Файл:

- `examples/23_timing/06_rtc_basic.py`

#### Резюме
- `RTC` е часовникът на борда за календарна дата и час, а не просто брояч на милисекунди.
- `rtc.datetime()` връща структурирано време, което може да се чете, отпечатва и задава внимателно.
- Предпазният флаг за задаване на време е добра практика, защото промяната на часовника е по-чувствителна от обикновен `print()`.

#### Самопроверка
1. Каква е разликата между `RTC` и таймер като `Timer(1)`?
2. Какво съдържа tuple-ът от `rtc.datetime()` и защо е полезно да го гледате структуриран?
3. Защо задаването на време трябва да стои зад предпазен флаг в учебен пример?

#### Упражнения
1. Отпечатвайте `rtc.datetime()` през една секунда няколко пъти и наблюдавайте кои полета се променят.
2. Направете четим формат `YYYY-MM-DD HH:MM:SS` от tuple-а на `rtc.datetime()`.
3. Включете задаване на време само през флаг и сравнете изхода преди и след настройката.

#### Ако не работи
- Симптом: стойностите на датата и часа изглеждат странно подредени. Проверка: припомнете си реда на полетата в tuple-а от `rtc.datetime()`.
- Симптом: времето се променя неочаквано по време на тест. Проверка: вижте дали не сте оставили флага за задаване на време включен.

#### Нататък
След тази глава преминете към „I2C master и runtime избор на пинове“, за да видите как времето и перифериите се срещат в реален сериен интерфейс.

## Част V: Серийни интерфейси

### Глава 19. I2C master и runtime избор на пинове

Базов пример:

```python
from machine import I2C  # Импортираме I2C за работа като master върху наличния хардуерен контролер.

i2c = I2C(1, freq=100000)  # Създаваме I2C(1) на 100 kHz върху бордовите пинове P100 и P101.
print(i2c.scan())  # Сканираме шината за устройства с 7-битов адрес.
```

Runtime пинове:

```python
from machine import I2C, Pin  # Импортираме I2C и Pin, за да покажем runtime избиране на SCL и SDA.

i2c = I2C(1, freq=400000, scl=Pin("P100"), sda=Pin("P101"))  # Създаваме I2C(1) на 400 kHz с изрично зададени пинове.
```

Тази глава е важна и заради конфликтите:

- `I2C(0)` ползва `P400/P401`
- `P400` е и `SW1`

Файлове:

- `examples/24_i2c/01_i2c_master_basic.py`
- `examples/24_i2c/02_i2c_runtime_pins.py`

#### Резюме
- `I2C` е двупроводна шина, в която master-ът управлява обмена, а устройствата отговарят по адрес.
- `scan()` е най-безопасният първи тест, защото проверява дали на шината изобщо има живи устройства.
- При този борд изборът на канал и пинове не е абстрактна подробност, а реален хардуерен конфликт, особено при `I2C(0)` и `SW1`.

#### Самопроверка
1. Каква е ролята на master-а в един `I2C` обмен?
2. Защо `scan()` е по-добър първи тест от директно четене на регистри от непознат модул?
3. Какъв практически проблем може да възникне, ако използвате `I2C(0)` и едновременно искате да работите със `SW1`?

#### Упражнения
1. Пуснете `scan()` при `100 kHz`, после при `400 kHz`, и сравнете дали резултатът остава стабилен.
2. Създайте втори пример с изрично зададени `scl` и `sda` и коментирайте защо това понякога е по-ясно от default конфигурацията.
3. Направете кратка таблица с ролите `SCL`, `SDA`, `master`, `device address`.

#### Ако не работи
- Симптом: няма отговор от шината. Проверка: първо сверете пиновете, адреса/ролята и нужните pull-up или control сигнали.
- Симптом: `scan()` връща празен списък. Проверка: устройството може да не е захранено, да е на грешни пинове или да има конфликт в окабеляването.
- Симптом: получавате нестабилни или безсмислени данни. Проверка: намалете скоростта и проверете окабеляването стъпка по стъпка.

#### Нататък
След тази глава преминете към „SoftI2C и I2CTarget“, за да сравните хардуерен и софтуерен подход към I2C комуникацията.

### Глава 20. SoftI2C и I2CTarget

SoftI2C:

```python
from machine import SoftI2C, Pin  # Импортираме SoftI2C и Pin за софтуерна I2C шина върху GPIO.

soft_i2c = SoftI2C(scl=Pin("P105", Pin.OPEN_DRAIN), sda=Pin("P104", Pin.OPEN_DRAIN), freq=100000)  # Създаваме SoftI2C на 100 kHz.
```

I2CTarget:

```python
from machine import I2CTarget, Pin  # Импортираме I2CTarget и Pin за работа в target или slave режим.

memory_buffer = bytearray(range(16))  # Подготвяме малък memory-backed буфер, който target-ът ще обслужва.
target = I2CTarget(1, addr=0x42, mem=memory_buffer, mem_addrsize=8, scl=Pin("P100"), sda=Pin("P101"))  # Създаваме I2CTarget(1) върху стандартните пинове на канал 1.
```

Тук се вижда:

- софтуерна I2C шина върху GPIO
- целево устройство с memory-backed буфер

Файлове:

- `examples/24_i2c/03_softi2c_basic.py`
- `examples/24_i2c/04_i2ctarget_memory.py`

#### Резюме
- `SoftI2C` показва, че I2C може да се реализира и върху GPIO, когато хардуерният контролер не е най-удобният избор.
- `I2CTarget` обръща гледната точка: тук бордът вече не е master, а устройство, което отговаря на адрес.
- Memory-backed буферът е добър учебен модел, защото прави протокола видим без сложен външен модул.

#### Самопроверка
1. Каква е практическата разлика между `SoftI2C` и хардуерен `I2C`?
2. Какво означава бордът да работи като `I2CTarget`, а не като master?
3. Защо memory-backed буферът е удобен начин за първи target пример?

#### Упражнения
1. Създайте малък буфер от 8 байта и проследете какво съдържание би виждал master-ът.
2. Променете `addr=0x42` на друга стойност и отбележете как това променя target идентичността.
3. Сравнете в коментар кога бихте предпочели `SoftI2C` и кога хардуерен `I2C`.

#### Ако не работи
- Симптом: няма отговор от шината. Проверка: първо сверете пиновете, адреса/ролята и нужните pull-up или control сигнали.
- Симптом: target-ът не отговаря. Проверка: вижте дали master и target са на една и съща шина и дали адресът съвпада с очаквания.
- Симптом: получавате нестабилни или безсмислени данни. Проверка: намалете скоростта и проверете окабеляването стъпка по стъпка.

#### Нататък
След тази глава преминете към „Hardware SPI и SoftSPI“, за да разширите темата от I2C към втори важен сериен интерфейс.

### Глава 21. Hardware SPI и SoftSPI

Hardware SPI:

```python
from machine import SPI, Pin  # Импортираме SPI и Pin за достъп до хардуерния SPI канал.

spi = SPI(0, baudrate=500000, polarity=0, phase=0, bits=8, sck=Pin("P102"), mosi=Pin("P101"), miso=Pin("P100"), cs=Pin("P103"))  # Създаваме SPI обект върху валидните board пинове.
```

SoftSPI:

```python
from machine import SoftSPI, Pin  # Импортираме SoftSPI и Pin за софтуерен SPI върху GPIO.

soft_spi = SoftSPI(baudrate=100000, polarity=0, phase=0, sck=Pin("P105"), mosi=Pin("P104"), miso=Pin("P106"))  # Създаваме софтуерен SPI обект върху три GPIO пина.
```

В тази книга `SPI` остава `1` канал.

Файлове:

- `examples/25_spi/01_spi_basic.py`
- `examples/25_spi/02_softspi_basic.py`

#### Резюме
- `SPI` е по-прост по идея от I2C, но изисква да сте много ясни за линиите `SCK`, `MOSI`, `MISO` и `CS`.
- Хардуерният `SPI` е естественият избор за скорост и стабилност, а `SoftSPI` е удобен за експерименти и нестандартно окабеляване.
- При `SPI` режимът (`polarity` и `phase`) и физическото loopback или устройство често са първото място, където се появява грешка.

#### Самопроверка
1. Каква е ролята на `MOSI`, `MISO`, `SCK` и `CS` в един SPI обмен?
2. Кога е по-разумно да използвате `SoftSPI`, вместо хардуерен `SPI`?
3. Защо грешен `polarity/phase` режим често води до „има трафик, но данните са грешни“?

#### Упражнения
1. Направете loopback тест, ако setup-ът го позволява, и сравнете изпратените и получените байтове.
2. Променете `baudrate` и вижте доколко обменът остава стабилен.
3. Изпробвайте различен `polarity/phase` режим и наблюдавайте как се променя смисълът на данните.

#### Ако не работи
- Симптом: няма отговор от шината. Проверка: първо сверете пиновете, адреса/ролята и нужните pull-up или control сигнали.
- Симптом: няма смислен отговор при loopback или модул. Проверка: вижте дали `MOSI` и `MISO` не са разменени и дали `CS` се управлява правилно.
- Симптом: получавате нестабилни или безсмислени данни. Проверка: намалете скоростта и проверете окабеляването стъпка по стъпка.

#### Нататък
След тази глава преминете към „TouchPad: базово четене“, за да приложите входното мислене върху по-специфичен сензорен ресурс.

## Част VI: TouchPad, памет и системни функции

### Глава 22. TouchPad: базово четене

Пример:

```python
from machine import Pin, TouchPad  # Импортираме Pin и TouchPad за работа с CTSU вход.

touch = TouchPad(Pin("P205"))  # Създаваме TouchPad върху валиден touch пин P205.
touch.config(500)  # Задаваме праг 500 за метода value().
print(touch.read())  # Четем необработената CTSU стойност.
print(touch.value())  # Преобразуваме стойността към 0 или 1 според прага.
print(touch.read_value())  # Взимаме едновременно count, value и последна FSP грешка.
```

Тук се виждат:

- сурова стойност
- логическа стойност по праг
- комбинирано четене

Файл:

- `examples/26_touchpad/01_touchpad_basic.py`

#### Резюме
- Главата развива темата „TouchPad: базово четене“ около по-чувствителни, по-системни или порт-специфични възможности.
- Тук вече не е достатъчно само да извикате API-то; важно е да разбирате ограниченията и режима на работа.
- Това е зоната, в която практическата дисциплина става толкова важна, колкото и самият пример.

#### Самопроверка
1. Коя е най-важната особеност или ограничение в „TouchPad: базово четене“?
2. Кой ресурс е volatile и кой е persistent в този тип пример?
3. Как бихте измерили или наблюдавали дали примерът се държи стабилно?

#### Упражнения
1. Добавете диагностичен печат или проста метрика, която да прави състоянието по-видимо.
2. Променете един ключов праг, буфер или режим и сравнете поведението преди и след това.
3. Свържете темата с реално действие на LED1, SW1 или малък лог в паметта, ако това е приложимо.

#### Ако не работи
- Симптом: поведението е чувствително или шумно. Проверка: търсете грешни прагове, неподходящ режим или неправилни очаквания към хардуера/порта.
- Симптом: резултатът не съвпада с интуицията. Проверка: разграничете RAM, постоянна памет, кеширане и порт-специфични особености.

#### Нататък
След тази глава преминете към „TouchPad: диагностика, cached API и асинхронно следене“, за да направите базовото четене по-стабилно и по-практично.

### Глава 23. TouchPad: диагностика, cached API и асинхронно следене

Диагностичен пример:

```python
TouchPad.sample_rate(20)  # Стартираме глобалния cooperative sampler на 20 пълни scan-а в секунда.
touch.start()  # Стартираме non-blocking scan, ако не е в ход такъв.
TouchPad.service()  # Придвижваме вътрешния sampler една стъпка от VM контекст.
print(touch.ready(), touch.read_cached(), touch.value_cached())  # Показваме cached състоянието.
print(touch.diagnose(8))  # Печатаме резултат от кратък диагностичен цикъл.
```

Асинхронен пример:

```python
TouchPad.sample_rate(50)  # Стартираме sampler на 50 scan-а в секунда.

async def touch_task():  # Асинхронна задача за следене на TouchPad.
    while True:  # Безкрайно следене.
        if tp.ready():  # Проверяваме дали пробата е готова.
            print(tp.read_cached(), tp.value_cached(), tp.age_ms())  # Печатаме cached стойностите.
        await asyncio.sleep_ms(20)  # Сканираме на 20 ms.
```

Тази глава е важна, защото показва не само „натиснато/ненатиснато“, а и сервизно поведение на CTSU слоя.

Файлове:

- `examples/26_touchpad/02_touchpad_diagnostics.py`
- `examples/04_touchpad_async.py`

#### Резюме
- Главата развива темата „TouchPad: диагностика, cached API и асинхронно следене“ около по-чувствителни, по-системни или порт-специфични възможности.
- Тук вече не е достатъчно само да извикате API-то; важно е да разбирате ограниченията и режима на работа.
- Това е зоната, в която практическата дисциплина става толкова важна, колкото и самият пример.

#### Самопроверка
1. Коя е най-важната особеност или ограничение в „TouchPad: диагностика, cached API и асинхронно следене“?
2. Кой ресурс е volatile и кой е persistent в този тип пример?
3. Как бихте измерили или наблюдавали дали примерът се държи стабилно?

#### Упражнения
1. Добавете диагностичен печат или проста метрика, която да прави състоянието по-видимо.
2. Променете един ключов праг, буфер или режим и сравнете поведението преди и след това.
3. Свържете темата с реално действие на LED1, SW1 или малък лог в паметта, ако това е приложимо.

#### Ако не работи
- Симптом: поведението е чувствително или шумно. Проверка: търсете грешни прагове, неподходящ режим или неправилни очаквания към хардуера/порта.
- Симптом: резултатът не съвпада с интуицията. Проверка: разграничете RAM, постоянна памет, кеширане и порт-специфични особености.

#### Нататък
След тази глава преминете към „`renesas.Flash`, `/flash` и `dataflash`“, за да видите как бордът пази данни и извън RAM.

### Глава 24. `renesas.Flash`, `/flash` и `dataflash`

Пример с `renesas.Flash`:

```python
import os  # Импортираме os, за да покажем съдържанието на файловата система /flash.
import renesas  # Импортираме renesas модула, който излага Flash block device.

flash = renesas.Flash()  # Създаваме block device обект върху вътрешната flash памет.
sector = bytearray(16)  # Подготвяме малък буфер за четене на първите байтове от блок 0.
flash.readblocks(0, sector)  # Четем първите 16 байта от блок 0 в буфера.
print(os.listdir("/flash"))  # Показваме кои файлове има във вътрешната файлова система.
```

Пример с `dataflash`:

```python
import dataflash  # Импортираме dataflash модула за достъп до отделната Data Flash област.

print(dataflash.size())  # Печатаме общия размер на Data Flash областта.
print(dataflash.block_size())  # Печатаме размера на erase блока.
print(dataflash.write_size())  # Печатаме минималния размер за запис.
print(dataflash.read(0, 16))  # Четем първите 16 байта от Data Flash.
```

Пример за устойчиво състояние в `dataflash` и видима индикация с `LED1`:

```python
from machine import Pin  # Импортираме Pin за управление на LED1.
import dataflash  # Импортираме dataflash за устойчиво съхранение на състояние.

led = Pin("LED1", Pin.OUT, value=1)  # Използваме board alias-а LED1, започваме изгасен.
state = dataflash.read(0, 1)[0]  # Връщаме стойността на първия байт.

if state == 0x00:  # Ако предишният старт е записал 0x00, сега правим erase към 0xFF.
    dataflash.erase_block(0)  # Изтриваме блок 0, всички байтове стават 0xFF.
    blinks_per_sec = 2  # Бавно мигане означава erase.
else:  # Иначе приемаме, че байтът е 0xFF и записваме 0x00.
    dataflash.write(0, bytes([0x00]))  # Записваме 0x00 в първия байт.
    blinks_per_sec = 10  # Бързо мигане означава write.
```

Този пример показва как един байт в `dataflash` може да пази състояние между два старта на програмата.
За `VK_RA4M2` LED индикацията е върху `LED1`, защото провереният board LED е `P204` и е active-low.
При всяко стартиране се редуват две състояния:

- `0xFF` след `erase_block(0)`
- `0x00` след `write(0, bytes([0x00]))`

Скоростта на мигане показва кой преход е станал на текущия boot.

Тук се разграничават:

- файлова система
- block device
- отделен data flash регион

Проверени размери за текущата конфигурация на `VK_RA4M2`:

- MCU вариантът на тази платка е с общо `384 KB` code flash и `128 KB` RAM
- в текущия linker layout `290 KB` са отделени за firmware/application region, а `94 KB` са отделени като `FLASH_FS` за `/flash`
- отделно има `8 KB` `dataflash`
- `/flash` е върху вътрешния flash block device, монтиран като `FAT`; block size е `512 bytes`
- реално свободното място за файлове е малко по-малко от `94 KB`, защото част отива за самата файлова система

Файлове:

- `examples/27_storage/01_flash_blockdev.py`
- `examples/27_storage/02_dataflash_basic.py`
- `examples/27_storage/03_dataflash_state_led.py`

#### Резюме
- Главата развива темата „`renesas.Flash`, `/flash` и `dataflash`“ около по-чувствителни, по-системни или порт-специфични възможности.
- Тук вече не е достатъчно само да извикате API-то; важно е да разбирате ограниченията и режима на работа.
- Това е зоната, в която практическата дисциплина става толкова важна, колкото и самият пример.

#### Самопроверка
1. Коя е най-важната особеност или ограничение в „`renesas.Flash`, `/flash` и `dataflash`“?
2. Кой ресурс е volatile и кой е persistent в този тип пример?
3. Как бихте измерили или наблюдавали дали примерът се държи стабилно?

#### Упражнения
1. Добавете диагностичен печат или проста метрика, която да прави състоянието по-видимо.
2. Променете един ключов праг, буфер или режим и сравнете поведението преди и след това.
3. Свържете темата с реално действие на LED1, SW1 или малък лог в паметта, ако това е приложимо.

#### Ако не работи
- Симптом: поведението е чувствително или шумно. Проверка: търсете грешни прагове, неподходящ режим или неправилни очаквания към хардуера/порта.
- Симптом: резултатът не съвпада с интуицията. Проверка: разграничете RAM, постоянна памет, кеширане и порт-специфични особености.

#### Нататък
След тази глава преминете към „Управление на паметта в MicroPython“, за да разберете как работата с буфери и памет влияе на реалните приложения.

### Глава 25. Управление на паметта в MicroPython

Диагностичен пример:

```python
import gc  # Импортираме gc за контрол и измерване на garbage-collected heap-а.
import micropython  # Импортираме micropython за mem_info и други runtime helper-и.

gc.collect()  # Пускаме GC в предвидим момент, за да започнем от по-чисто състояние.
print(gc.mem_free(), gc.mem_alloc())  # Печатаме свободния и заетия heap.
micropython.mem_info()  # Печатаме обобщена информация за heap-а.
```

Буферен пример:

```python
buffer = bytearray(32)  # Създаваме един буфер, който ще преизползваме.
window = memoryview(buffer)[8:12]  # Вземаме прозорец към част от буфера без копиране.
window[0] = 123  # Променяме първия байт през memoryview.
print(list(buffer[8:12]))  # Показваме, че оригиналният буфер е променен на място.
```

Тук „управление на паметта“ не означава ръчно местене на адреси както в C, а по-скоро:

- да разбираме кои обекти заемат място в heap-а на MicroPython
- да различаваме RAM heap от stack, firmware и постоянна flash памет
- да ограничаваме излишните алокации в чувствителни по време участъци
- да преизползваме буфери вместо да създаваме нови обекти във всеки цикъл

Потвърдени факти за текущия `VK_RA4M2` порт:

- garbage-collected heap-ът се подава на MicroPython чрез linker символите `_heap_start` и `_heap_end`
- linker-ът заделя отделен фиксиран stack регион от `8 KB`
- `/flash` и `dataflash` са постоянна памет за файлове и данни, а не RAM heap за Python обекти

Практични API-та, които реално са налични в този порт:

- `gc.collect()` за принудително стартиране на garbage collector в удобен момент
- `gc.mem_free()` и `gc.mem_alloc()` за ориентир колко heap е свободен и колко е зает
- `gc.threshold()` за по-напреднал контрол кога да се задейства GC
- `micropython.mem_info()` и `micropython.mem_info(1)` за по-подробна картина на heap-а
- `micropython.alloc_emergency_exception_buf()` за изключително важни exception-и в interrupt/critical контекст
- `micropython.heap_lock()` и `micropython.heap_unlock()` за напреднали случаи, когато временно не трябва да се допуска алокация

Практически правила, които най-често дават реален ефект:

- правете `bytearray` веднъж и после го пълнете отново, вместо да създавате нов буфер всеки път
- използвайте `memoryview`, когато искате прозорец към част от буфера без копиране
- ако имате временни големи обекти, освобождавайте ги и извиквайте `gc.collect()` в предвидим момент, а не насред критична операция
- при interrupt-и, callbacks и latency-sensitive код избягвайте нови алокации; подгответе буферите предварително
- не смесвайте `machine.mem8/mem16/mem32` с управлението на Python heap-а; това са различни нива на достъп до паметта

Тази тема се свързва естествено с вече наличните примери:

- `07_pointers_equivalents.py` показва shared reference и `memoryview`
- `04_i2ctarget_memory.py` показва предварително заделен mutable буфер, подаден към периферен API
- `01_flash_blockdev.py` и `02_dataflash_basic.py` показват, че persistent storage не е същото като heap RAM

Файлове:

- `examples/28_machine_misc/04_memory_management.py`
- `examples/20_language_basics/07_pointers_equivalents.py`
- `examples/24_i2c/04_i2ctarget_memory.py`
- `examples/27_storage/01_flash_blockdev.py`
- `examples/27_storage/02_dataflash_basic.py`

#### Резюме
- Главата развива темата „Управление на паметта в MicroPython“ около по-чувствителни, по-системни или порт-специфични възможности.
- Тук вече не е достатъчно само да извикате API-то; важно е да разбирате ограниченията и режима на работа.
- Това е зоната, в която практическата дисциплина става толкова важна, колкото и самият пример.

#### Самопроверка
1. Коя е най-важната особеност или ограничение в „Управление на паметта в MicroPython“?
2. Кой ресурс е volatile и кой е persistent в този тип пример?
3. Как бихте измерили или наблюдавали дали примерът се държи стабилно?

#### Упражнения
1. Добавете диагностичен печат или проста метрика, която да прави състоянието по-видимо.
2. Променете един ключов праг, буфер или режим и сравнете поведението преди и след това.
3. Свържете темата с реално действие на LED1, SW1 или малък лог в паметта, ако това е приложимо.

#### Ако не работи
- Симптом: поведението е чувствително или шумно. Проверка: търсете грешни прагове, неподходящ режим или неправилни очаквания към хардуера/порта.
- Симптом: резултатът не съвпада с интуицията. Проверка: разграничете RAM, постоянна памет, кеширане и порт-специфични особености.

#### Нататък
След тази глава преминете към „`machine` модулът и порт-специфично поведение“, за да слезете още едно ниво по-близо до самия порт и хардуер.

### Глава 26. `machine` модулът и порт-специфично поведение

Информационен пример:

```python
import machine  # Импортираме machine, за да покажем системните функции на порта.

uid = machine.unique_id()  # Четем уникалния 128-битов идентификатор на MCU.
uid_hex = "".join("{:02x}".format(byte_value) for byte_value in uid)  # Преобразуваме байтовете до четим hex низ.

print("machine.freq() =", machine.freq())  # Показваме текущата честота на ядрото.
print("machine.unique_id() =", uid_hex)  # Печатаме уникалния идентификатор в hex вид.
print("machine.reset_cause() =", machine.reset_cause())  # Печатаме причината за последния reset.
machine.info()  # Печатаме подробна системна информация от самия порт.
```

Ниско ниво:

```python
irq_state = machine.disable_irq()  # Временно забраняваме прекъсванията и пазим предишното състояние.
machine.enable_irq(irq_state)  # Възстановяваме прекъсванията веднага след демонстрацията.
print(hasattr(machine, "mem32"))  # Показваме дали портът излага 32-битов memory access.
```

Sleep режими:

```python
DO_LIGHTSLEEP = False  # Оставяме lightsleep изключен по подразбиране.
DO_DEEPSLEEP = False  # Оставяме deepsleep изключен по подразбиране.

print("Извикваме кратък machine.lightsleep(20).")  # Подготвяме потребителя за кратък sleep.
machine.lightsleep(20)  # Извикваме кратък lightsleep за 20 ms.

if DO_LIGHTSLEEP:  # Само ако е включено, пробваме по-дълъг lightsleep.
    machine.lightsleep(100)  # Извикваме lightsleep за 100 ms.
```

Тази глава е мястото за:

- `machine.info`
- `machine.freq`
- `machine.unique_id`
- `machine.reset_cause`
- `disable_irq` и `enable_irq`
- `mem8`, `mem16`, `mem32`
- предпазен `machine.bootloader`
- `sleep`, `lightsleep`, `deepsleep`

Практическа бележка за честоти и ток:

- `machine.freq()` е полезен за бърза проверка на runtime честотата; текущата board конфигурация е `100 MHz` системен clock и `50 MHz` периферни clock-и за много блокове
- токовите числа в hardware manual-а са за самия MCU при конкретни условия, а не за цялата платка през USB
- типични стойности от manual-а са около `15.4 mA` в normal mode с всички peripheral clocks enabled, `6.1 mA` при disabled peripheral clocks и `4.4 mA` в sleep mode
- за ниска консумация manual-ът дава ориентири от `0.7 uA` за software standby и приблизително `5 ... 16 uA` за deep software standby според retained функциите
- по време на `12-bit` A/D conversion analog current е типично около `0.8 mA`, което е отделна аналогова консумация и не бива да се бърка с общата консумация на платката

Файлове:

- `examples/28_machine_misc/01_machine_info_and_id.py`
- `examples/28_machine_misc/02_machine_mem_and_bootloader.py`
- `examples/28_machine_misc/03_sleep_modes.py`

#### Резюме
- Тук `machine` вече не е просто помощен модул, а мястото, където се вижда самоличността и ниското ниво на самия порт.
- `machine.info()`, `freq()`, `unique_id()` и `reset_cause()` са полезни за диагностика, инвентаризация и проверка на runtime състоянието.
- `disable_irq()`, `mem32`, `bootloader`, `lightsleep()` и `deepsleep()` изискват повече дисциплина, защото работят близо до системното поведение на борда.
- Предпазните флагове в примерите не са излишни, а начин да упражнявате чувствителни API-та без излишен риск.

#### Самопроверка
1. Кога бихте ползвали `machine.info()` и кога `machine.unique_id()`?
2. Защо след `disable_irq()` е важно да възстановите точно предишното състояние с `enable_irq(irq_state)`?
3. Защо `DO_LIGHTSLEEP`, `DO_DEEPSLEEP` и подобни флагове са добра учебна практика при чувствителни операции?

#### Упражнения
1. Направете кратък „паспорт на борда“, който печата `freq`, `unique_id`, `reset_cause` и няколко реда от `machine.info()`.
2. Измерете реалното време около `machine.lightsleep(20)` с `time.ticks_ms()` и сравнете резултата с подадената стойност.
3. Оставете рискована операция като `bootloader()` или по-дълбок sleep зад булев флаг и тествайте първо само безопасния клон.

#### Ако не работи
- Симптом: бордът изглежда „замръзва“ или сменя режима неочаквано. Проверка: вижте дали не сте включили `bootloader`, `deepsleep` или по-агресивен sleep сценарий.
- Симптом: low-level частта дава странни резултати. Проверка: разграничете нормален Python обект от директен достъп през `mem8/mem16/mem32` и проверете дали наистина ви трябва ниско ниво.
- Симптом: след експеримент с прекъсванията поведението става непредвидимо. Проверка: уверете се, че `enable_irq()` възстановява предишното състояние, а не произволна стойност.

#### Нататък
След тази глава преминете към „Динамична 7-сегментна индикация и матрично сканиране“, където системното мислене за timing и ресурси вече се превръща в реална периферна организация.

## Част VII: Индикация и входни матрици

### Глава 27. Динамична 7-сегментна индикация и матрично сканиране

Пример:

```python
from machine import Pin  # Импортираме Pin, за да покажем базовото сканиране на външен дисплей и бутони.
import time  # Импортираме time за краткото динамично опресняване.

segment_pins = [Pin(name, Pin.OUT) for name in ("P105", "P104", "P113", "P114", "P115", "P304", "P303")]  # Това са седемте сегмента a..g.
digit_pins = [Pin(name, Pin.OUT) for name in ("P608", "P409")]  # Това са двата общи извода за две цифри.
patterns = {"1": (0, 1, 1, 0, 0, 0, 0), "2": (1, 1, 0, 1, 1, 0, 1)}  # Това са сегментните шаблони за две примерни цифри.

for refresh_step in range(10):  # Повтаряме кратък опреснителен цикъл десет пъти.
    for digit_index, symbol in enumerate(("1", "2")):  # Показваме символ 1 на първата цифра и 2 на втората цифра.
        for digit_pin in digit_pins:  # Първо изключваме всички цифри, за да няма ghosting.
            digit_pin.off()  # Изключваме текущата цифра.
        for segment_pin, level in zip(segment_pins, patterns[symbol]):  # Зареждаме нужния сегментен шаблон за текущия символ.
            segment_pin.value(level)  # Настройваме сегмента според шаблона.
        digit_pins[digit_index].on()  # Включваме само текущата цифра за кратко време.
        time.sleep_ms(5)  # Държим цифрата включена кратко, за да се получи динамична индикация.
```

Клавиатурна матрица:

```python
row_pins = [Pin(name, Pin.OUT) for name in ("P408", "P600")]  # Това са редовете на примерна 2x2 клавиатурна матрица.
col_pins = [Pin(name, Pin.IN, Pin.PULL_UP) for name in ("P301", "P302")]  # Това са колоните на примерната 2x2 клавиатурна матрица.
pressed_keys = []  # Подготвяме списък за натиснати клавиши от матрицата.

for row_index, row_pin in enumerate(row_pins):  # Сканираме всеки ред поотделно.
    for reset_row in row_pins:  # Първо изключваме всички редове.
        reset_row.off()  # Изключваме реда.
    row_pin.on()  # Активираме текущия ред.
    for col_index, col_pin in enumerate(col_pins):  # Проверяваме всяка колона в активния ред.
        if col_pin.value() == 0:  # Ако колоната падне на 0, приемаме че бутонът е натиснат.
            pressed_keys.append((row_index, col_index))  # Записваме координатите на натиснатия бутон.
```

Този пример изисква външен хардуер, но е много важен за свързване на GPIO, време и логика.

Файл:

- `examples/29_display_scan/01_dynamic_7seg_and_keypad.py`

#### Резюме
- Главата комбинира GPIO, време и външен хардуер в една по-сложна, но много показателна демонстрация.
- Основната идея е, че мултиплексирането и матричното сканиране разчитат на правилен ред и темпо на опресняване.
- Това е добър пример как софтуерното време пряко влияе на видимия хардуерен резултат.

#### Самопроверка
1. Защо динамичната индикация не работи добре при твърде бавно опресняване?
2. Каква е идеята зад матричното сканиране на клавиши?
3. Кой проблем ще се появи първо, ако редът на управление на сегменти и цифри е грешен?

#### Упражнения
1. Променете честотата на опресняване и наблюдавайте кога започва видимо мигане.
2. Добавете проста таблица за превод от координати на матрицата към символи.
3. Използвайте LED1 като диагностичен индикатор при намерен натиснат клавиш.

#### Ако не работи
- Симптом: дисплеят е слаб или премигва. Проверка: периодът на опресняване вероятно е твърде дълъг или кодът блокира между циклите.
- Симптом: разчитането на клавишите е грешно. Проверка: прегледайте съответствието между редове, колони и реалното окабеляване.

#### Нататък
След тази глава преминете към „Минимална FSM и таблична FSM“, за да превърнете входовете, изходите и времето в по-структурирана логика.

## Част VIII: FSM като начин на мислене

### Глава 28. Минимална FSM и таблична FSM

Минимална FSM:

```python
STATE_OFF = "OFF"  # Това е първото състояние на лампата.
STATE_ON = "ON"  # Това е второто състояние на лампата.
EVENT_BUTTON = "BUTTON"  # Това е събитието от бутона.

state = STATE_OFF  # Започваме с изключена лампа.

def handle_event(current_state, event_name):  # Дефинираме преходната функция на машината на състояния.
    if current_state == STATE_OFF and event_name == EVENT_BUTTON:  # Ако сме в OFF и бутонът е натиснат, минаваме в ON.
        return STATE_ON  # Връщаме новото състояние ON.
    if current_state == STATE_ON and event_name == EVENT_BUTTON:  # Ако сме в ON и бутонът е натиснат, минаваме в OFF.
        return STATE_OFF  # Връщаме новото състояние OFF.
    return current_state  # Ако няма преход, оставаме в същото състояние.
```

Таблична FSM:

```python
state = "OFF"  # Започваме в състояние OFF.

transition_table = {("OFF", "BUTTON"): "ON", ("ON", "BUTTON"): "OFF"}  # В таблицата описваме преходите като двойка от state и event.

event_sequence = ["BUTTON", "BUTTON", "BUTTON", "BUTTON"]  # Симулираме поредица от събития.
```

Графично идеята може да се мисли така:

```text
OFF --BUTTON--> ON
ON  --BUTTON--> OFF
```

Файлове:

- `examples/30_fsm/01_fsm_lamp_button.py`
- `examples/30_fsm/02_fsm_table.py`

#### Резюме
- Главата развива системното мислене чрез темата „Минимална FSM и таблична FSM“.
- Основният акцент е върху разделяне на състояния, събития, условия, приоритети и срокове.
- Това е архитектурен материал: целта е не само да работи, а да остане четим и разширяем.

#### Самопроверка
1. Коя е ключовата моделна идея в „Минимална FSM и таблична FSM“?
2. Как тази глава прави поведението по-структурирано от чиста поредица от `if` проверки?
3. Кое решение тук помага най-много за бъдещо разширяване на системата?

#### Упражнения
1. Добавете още едно състояние, събитие, guard или правило според темата на главата.
2. Отпечатвайте текущото състояние и последното обработено събитие за по-лесно проследяване.
3. Опишете с кратка диаграма или таблица как бихте разширили модела с още един реален вход.

#### Ако не работи
- Симптом: системата влиза в неочаквано състояние. Проверка: търсете липсващ преход, грешен приоритет или слабо дефиниран event/condition.
- Симптом: моделът става нечетим. Проверка: върнете се към ясни имена на states, events и отделни помощни функции или таблици.

#### Нататък
След тази глава преминете към „FSM с timeout“, за да добавите време като част от самата логика на състоянията.

### Глава 29. FSM с timeout

Пример:

```python
state = "OFF"  # Започваме с изключена лампа.

transition_table = {("OFF", "BUTTON"): "ON", ("ON", "TIMEOUT"): "OFF", ("ON", "BUTTON"): "OFF"}  # Описваме преходите с timeout.

event_sequence = ["BUTTON", "TIMEOUT", "BUTTON", "BUTTON"]  # Симулираме бутон и timeout събития.
```

Тази глава е преходът от „лампа + бутон“ към „лампа + бутон + време“.

Файл:

- `examples/30_fsm/03_fsm_timeout.py`

#### Резюме
- Главата развива системното мислене чрез темата „FSM с timeout“.
- Основният акцент е върху разделяне на състояния, събития, условия, приоритети и срокове.
- Това е архитектурен материал: целта е не само да работи, а да остане четим и разширяем.

#### Самопроверка
1. Коя е ключовата моделна идея в „FSM с timeout“?
2. Как тази глава прави поведението по-структурирано от чиста поредица от `if` проверки?
3. Кое решение тук помага най-много за бъдещо разширяване на системата?

#### Упражнения
1. Добавете още едно състояние, събитие, guard или правило според темата на главата.
2. Отпечатвайте текущото състояние и последното обработено събитие за по-лесно проследяване.
3. Опишете с кратка диаграма или таблица как бихте разширили модела с още един реален вход.

#### Ако не работи
- Симптом: системата влиза в неочаквано състояние. Проверка: търсете липсващ преход, грешен приоритет или слабо дефиниран event/condition.
- Симптом: моделът става нечетим. Проверка: върнете се към ясни имена на states, events и отделни помощни функции или таблици.

#### Нататък
След тази глава преминете към „Conditions vs Events“, за да видите по-ясно кога да мислите в условия и кога в събития.

### Глава 30. Conditions vs Events

Пример:

```python
light_level = 600  # Това е моментното ниво от светлинния сензор в симулацията.
dark_threshold = 800  # Това е прагът, под който приемаме че е тъмно.
event_name = "BUTTON"  # Това е дискретното събитие от бутон.

condition_is_dark = light_level < dark_threshold  # Това е condition, която може да е вярна дълго време.

if condition_is_dark and event_name == "BUTTON":  # Комбинираме условие и събитие в общо решение.
    print("Лампата може да се включи, защото е тъмно и има бутонно събитие.")  # Печатаме положителния изход.
else:  # Ако комбинацията не е изпълнена, не включваме лампата.
    print("Няма достатъчно условия за включване.")  # Печатаме отрицателния изход.
```

Тук разграничението е:

- condition може да е вярно дълго време
- event е дискретен факт

Файл:

- `examples/30_fsm/04_conditions_vs_events.py`

#### Резюме
- Главата развива системното мислене чрез темата „Conditions vs Events“.
- Основният акцент е върху разделяне на състояния, събития, условия, приоритети и срокове.
- Това е архитектурен материал: целта е не само да работи, а да остане четим и разширяем.

#### Самопроверка
1. Коя е ключовата моделна идея в „Conditions vs Events“?
2. Как тази глава прави поведението по-структурирано от чиста поредица от `if` проверки?
3. Кое решение тук помага най-много за бъдещо разширяване на системата?

#### Упражнения
1. Добавете още едно състояние, събитие, guard или правило според темата на главата.
2. Отпечатвайте текущото състояние и последното обработено събитие за по-лесно проследяване.
3. Опишете с кратка диаграма или таблица как бихте разширили модела с още един реален вход.

#### Ако не работи
- Симптом: системата влиза в неочаквано състояние. Проверка: търсете липсващ преход, грешен приоритет или слабо дефиниран event/condition.
- Симптом: моделът става нечетим. Проверка: върнете се към ясни имена на states, events и отделни помощни функции или таблици.

#### Нататък
След тази глава преминете към „Guard функции“, за да направите преходите между състояния по-умни и по-контролирани.

### Глава 31. Guard функции

Пример:

```python
context = {"light": 500, "pir": True}  # Подготвяме контекст със стойности от сензори.
state = "OFF"  # Започваме в състояние OFF.
event_name = "MOTION"  # Симулираме събитие за засечено движение.

def is_dark(data):  # Това е първата guard функция.
    return data["light"] < 800  # Връщаме True само ако е достатъчно тъмно.

def has_motion(data):  # Това е втората guard функция.
    return data["pir"] is True  # Връщаме True само ако PIR сензорът вижда движение.

if state == "OFF" and event_name == "MOTION" and is_dark(context) and has_motion(context):  # Комбинираме event, state и guard функции.
    state = "ON"  # Минаваме в ON само ако всички проверки са успешни.
```

Тук преходът не зависи само от state и event, а и от допълнителни условия.

Файл:

- `examples/30_fsm/05_guard_functions.py`

#### Резюме
- Главата развива системното мислене чрез темата „Guard функции“.
- Основният акцент е върху разделяне на състояния, събития, условия, приоритети и срокове.
- Това е архитектурен материал: целта е не само да работи, а да остане четим и разширяем.

#### Самопроверка
1. Коя е ключовата моделна идея в „Guard функции“?
2. Как тази глава прави поведението по-структурирано от чиста поредица от `if` проверки?
3. Кое решение тук помага най-много за бъдещо разширяване на системата?

#### Упражнения
1. Добавете още едно състояние, събитие, guard или правило според темата на главата.
2. Отпечатвайте текущото състояние и последното обработено събитие за по-лесно проследяване.
3. Опишете с кратка диаграма или таблица как бихте разширили модела с още един реален вход.

#### Ако не работи
- Симптом: системата влиза в неочаквано състояние. Проверка: търсете липсващ преход, грешен приоритет или слабо дефиниран event/condition.
- Симптом: моделът става нечетим. Проверка: върнете се към ясни имена на states, events и отделни помощни функции или таблици.

#### Нататък
След тази глава преминете към „Приоритети и event queue“, за да организирате събитията, когато вече не са само едно по едно.

### Глава 32. Приоритети и event queue

Приоритети:

```python
pending_events = ["BUTTON", "ERROR", "TIMEOUT"]  # Симулираме едновременно чакащи събития.
priority_order = {"ERROR": 0, "BUTTON": 1, "TIMEOUT": 2}  # По-малкото число означава по-висок приоритет.

pending_events.sort(key=lambda event_name: priority_order[event_name])  # Подреждаме събитията по приоритет.
```

Event queue:

```python
event_queue = []  # Създаваме проста event queue като обикновен Python list.

event_queue.append(("BUTTON", {"source": "SW1"}))  # Добавяме събитие от бутон.
event_queue.append(("PIR", {"source": "pir_sensor"}))  # Добавяме събитие от PIR сензор.
event_queue.append(("TIMEOUT", {"ms": 30000}))  # Добавяме timeout събитие.

while event_queue:  # Обработваме опашката, докато има чакащи събития.
    event_name, payload = event_queue.pop(0)  # Взимаме най-старото събитие от началото на опашката.
    print("Обработваме", event_name, "с payload", payload)  # Печатаме кое събитие се обработва сега.
```

Това е мостът между:

- прекъсвания
- таймери
- сензори
- FSM логика

Файлове:

- `examples/30_fsm/06_priorities.py`
- `examples/30_fsm/07_event_queue.py`

#### Резюме
- Главата развива системното мислене чрез темата „Приоритети и event queue“.
- Основният акцент е върху разделяне на състояния, събития, условия, приоритети и срокове.
- Това е архитектурен материал: целта е не само да работи, а да остане четим и разширяем.

#### Самопроверка
1. Коя е ключовата моделна идея в „Приоритети и event queue“?
2. Как тази глава прави поведението по-структурирано от чиста поредица от `if` проверки?
3. Кое решение тук помага най-много за бъдещо разширяване на системата?

#### Упражнения
1. Добавете още едно състояние, събитие, guard или правило според темата на главата.
2. Отпечатвайте текущото състояние и последното обработено събитие за по-лесно проследяване.
3. Опишете с кратка диаграма или таблица как бихте разширили модела с още един реален вход.

#### Ако не работи
- Симптом: системата влиза в неочаквано състояние. Проверка: търсете липсващ преход, грешен приоритет или слабо дефиниран event/condition.
- Симптом: моделът става нечетим. Проверка: върнете се към ясни имена на states, events и отделни помощни функции или таблици.

#### Нататък
След тази глава преминете към „Tickless timers и sleep идея“, за да видите как FSM мисленето може да се свърже и с икономия на време и енергия.

### Глава 33. Tickless timers и sleep идея

Пример:

```python
import machine  # Импортираме machine за кратък lightsleep между събитията.
import time  # Импортираме time за работа с ticks_ms и ticks_diff.

tasks = [("lamp_timeout", 120), ("fan_timeout", 450), ("display_refresh", 40)]  # Подготвяме три задачи с различни крайни срокове в милисекунди.
start_ms = time.ticks_ms()  # Запомняме началния момент.
next_deadline_ms = min(deadline for _, deadline in tasks)  # Избираме най-близкия срок като tickless цел.
sleep_ms = max(0, next_deadline_ms - time.ticks_diff(time.ticks_ms(), start_ms))  # Изчисляваме колко време може да се спи до следващото събитие.
machine.lightsleep(sleep_ms)  # Спим точно до най-близкото планирано събитие с lightsleep.
elapsed_ms = time.ticks_diff(time.ticks_ms(), start_ms)  # Изчисляваме изминалото време след съня.
ready_tasks = [task_name for task_name, deadline in tasks if deadline <= elapsed_ms]  # Избираме всички задачи, които вече са изтекли.
```

Тази глава не разчита на агресивни sleep режими, а показва начина на мислене:

- намираме най-близкия срок
- спим точно дотогава
- обработваме всички изтекли задачи

Файлове:

- `examples/30_fsm/08_tickless_sleep.py`
- `examples/28_machine_misc/03_sleep_modes.py`

#### Резюме
- Главата развива системното мислене чрез темата „Tickless timers и sleep идея“.
- Основният акцент е върху разделяне на състояния, събития, условия, приоритети и срокове.
- Това е архитектурен материал: целта е не само да работи, а да остане четим и разширяем.

#### Самопроверка
1. Коя е ключовата моделна идея в „Tickless timers и sleep идея“?
2. Как тази глава прави поведението по-структурирано от чиста поредица от `if` проверки?
3. Кое решение тук помага най-много за бъдещо разширяване на системата?

#### Упражнения
1. Добавете още едно състояние, събитие, guard или правило според темата на главата.
2. Отпечатвайте текущото състояние и последното обработено събитие за по-лесно проследяване.
3. Опишете с кратка диаграма или таблица как бихте разширили модела с още един реален вход.

#### Ако не работи
- Симптом: системата влиза в неочаквано състояние. Проверка: търсете липсващ преход, грешен приоритет или слабо дефиниран event/condition.
- Симптом: моделът става нечетим. Проверка: върнете се към ясни имена на states, events и отделни помощни функции или таблици.

#### Нататък
След тази глава преминете към „Управление на осветление и вентилация за баня“, за да приложите FSM идеите върху цялостен реален сценарий.

## Част IX: Реален пример

### Глава 34. Управление на осветление и вентилация за баня

Пример:

```python
from machine import Pin  # Импортираме Pin за реален хардуерен достъп.

led = Pin("LED1", Pin.OUT, value=1)  # LED1 = P204, active-low: value=1 означава изгасен.
button = Pin("SW1", Pin.IN, Pin.PULL_UP)  # SW1 = P400, active-low: value=0 означава натиснат.

sensors = {"pir": True, "door_open": False, "light_level": 300, "humidity": 82}  # Примерни входни данни от четири сензора.
outputs = {"lamp": False, "fan": False}  # Два управлявани изхода за лампа и вентилатор.
button_pressed = (button.value() == 0)  # SW1 е active-low: 0 означава натиснат.

if sensors["pir"] and sensors["light_level"] < 800:  # Ако има движение и е тъмно, включваме лампата.
    outputs["lamp"] = True  # Включваме лампата.
if button_pressed:  # Ако бутонът е натиснат, също включваме лампата.
    outputs["lamp"] = True  # Ръчно включване от потребителя.
if sensors["humidity"] > 75 or sensors["door_open"]:  # Ако влажността е висока или вратата е отворена, включваме вентилатора.
    outputs["fan"] = True  # Включваме вентилатора.
```

Това е умишлено учебна симулация, не готов продукт.

Но тя събира:

- движение
- врата
- осветеност
- влажност
- два управлявани изхода

Файл:

- `examples/31_case_study/01_bathroom_control.py`

#### Резюме
- Главата прилага вече натрупаните идеи в по-реалистичен сценарий чрез „Управление на осветление и вентилация за баня“.
- Тук вече се мисли за цялостна система, а не за изолиран синтактичен или периферен пример.
- Практическата стойност е в това да видите как отделни техники започват да работят заедно.

#### Самопроверка
1. Коя част от „Управление на осветление и вентилация за баня“ е реална и коя е умишлено симулирана за учебни цели?
2. Кои решения в тази глава са добри за учебен пример, но още не са завършен продукт?
3. Кой предишен урок се използва най-видимо тук?

#### Упражнения
1. Променете един праг или правило и опишете как това влияе на цялата система.
2. Добавете кратък лог на решенията, които системата взема.
3. Разделете една част от логиката в помощна функция, за да направите кода по-четим.

#### Ако не работи
- Симптом: логиката изглежда твърде сложна. Проверка: проследявайте отделно входовете, условията и изходите, вместо да гледате всичко наведнъж.
- Симптом: резултатът не отговаря на очакванията. Проверка: вижте кои входове са реални и кои са само симулирани данни в примера.

#### Нататък
След тази глава преминете към „Декомпозиция на две FSM“, за да видите как по-сложната задача се разделя на по-малки и по-ясни машини.

### Глава 35. Декомпозиция на две FSM

Пример:

```python
lamp_state = "OFF"  # Това е текущото състояние на FSM за лампата.
fan_state = "OFF"  # Това е текущото състояние на FSM за вентилатора.

event_sequence = []  # Започваме с празен списък.
if button.value() == 0:  # Проверяваме дали SW1 е натиснат в момента.
    event_sequence.append("BUTTON")  # Добавяме реално събитие от бутона.
event_sequence.extend(["HUMIDITY_HIGH", "TIMEOUT_LAMP", "TIMEOUT_FAN"])  # Добавяме симулирани събития.

for event_name in event_sequence:  # Обработваме събитията последователно и за двете машини.
    if event_name == "BUTTON":  # Бутонът управлява лампата.
        lamp_state = "ON" if lamp_state == "OFF" else "OFF"  # Превключваме FSM на лампата.
    if event_name == "HUMIDITY_HIGH":  # Високата влажност влияе на FSM на вентилатора.
        fan_state = "ON"  # Включваме вентилатора.
```

Тук голямата идея е:

- една FSM да не поглъща цялата система
- отделните машини да имат отделни отговорности
- комуникацията да става през събития

Файл:

- `examples/31_case_study/02_two_fsms.py`

Забележка: И двата case study примера вече използват реален LED1 (P204) и SW1 (P400) за вход и изход, освен симулираните сензори.

#### Резюме
- Главата прилага вече натрупаните идеи в по-реалистичен сценарий чрез „Декомпозиция на две FSM“.
- Тук вече се мисли за цялостна система, а не за изолиран синтактичен или периферен пример.
- Практическата стойност е в това да видите как отделни техники започват да работят заедно.

#### Самопроверка
1. Коя част от „Декомпозиция на две FSM“ е реална и коя е умишлено симулирана за учебни цели?
2. Кои решения в тази глава са добри за учебен пример, но още не са завършен продукт?
3. Кой предишен урок се използва най-видимо тук?

#### Упражнения
1. Променете един праг или правило и опишете как това влияе на цялата система.
2. Добавете кратък лог на решенията, които системата взема.
3. Разделете една част от логиката в помощна функция, за да направите кода по-четим.

#### Ако не работи
- Симптом: логиката изглежда твърде сложна. Проверка: проследявайте отделно входовете, условията и изходите, вместо да гледате всичко наведнъж.
- Симптом: резултатът не отговаря на очакванията. Проверка: вижте кои входове са реални и кои са само симулирани данни в примера.

#### Нататък
След тази глава преминете към „Базова UART комуникация“, за да добавите още един практически канал за обмен на данни.

## Част X: UART комуникация

### Глава 36. Базова UART комуникация

Пример:

```python
from machine import UART  # Импортираме UART класа за серийна комуникация.
import time  # Импортираме time за кратко закъснение.

uart = UART(9, 115200)  # Създаваме UART(9) на 115200 baud (TX=P602, RX=P601).
message = "Здравей от VK_RA4M2!\r\n"  # Подготвяме съобщение за изпращане.
bytes_sent = uart.write(message)  # Изпращаме съобщението по TX пина.

time.sleep_ms(100)  # Кратко закъснение, за да пристигнат данни (ако има loopback).
available = uart.any()  # Проверяваме дали има налични байтове за четене.
if available > 0:  # Ако има данни за четене:
    received = uart.read(available)  # Четем наличните байтове от RX буфера.
    print("Получено:", received)  # Показваме получените данни.
```

Налични UART канали:

- `UART(0)` -> `TX=P411`, `RX=P410`
- `UART(2)` -> `TX=P302`, `RX=P301`
- `UART(7)` -> `TX=P401`, `RX=P402`, `CTS=P403`
- `UART(9)` -> `TX=P602`, `RX=P601`, `CTS=P603`

Тук се учат:

- създаване на UART инстанция
- изпращане и четене на данни
- loopback тест (TX свързан към RX)

Файл:

- `examples/32_uart/01_uart_basic.py`

#### Резюме
- Главата въвежда UART като най-простия сериен канал за текст и байтове.
- Показани са базово изпращане, наличие на данни и четене от буфера.
- Loopback тестът е естественият първи sanity check преди по-сложна комуникация.

#### Самопроверка
1. Каква е ролята на baud rate при UART комуникацията?
2. Защо loopback тестът е добър първи тест на конфигурацията?
3. Каква е практическата разлика между `any()` и `read()`?

#### Упражнения
1. Изпращайте прост текстов брояч веднъж в секунда по UART.
2. Ако имате loopback, отпечатвайте обратно полученото съобщение като echo тест.
3. Добавете функция за изпращане на текстов ред с край `\r\n`.

#### Ако не работи
- Симптом: текстът е нечетим. Проверка: baud rate и настройките от двете страни трябва да съвпадат.
- Симптом: няма върнати данни. Проверка: при loopback TX и RX трябва да са свързани по очаквания за теста начин.

#### Нататък
След тази глава преминете към „Работа с външни модули по интерфейси“, за да надградите голото серийно предаване до реална интеграция на устройства.

## Част XI: Външни модули по интерфейси

### Глава 37. Работа с външни модули по интерфейси

Тази глава не въвежда нов хардуерен контролер.

Тя събира вече видените `I2C`, `SPI` и `UART` в един практичен модел за мислене.

Когато включвате външен модул, не започвате от „готов driver“, а от четири по-базови въпроса:

1. С какво напрежение и логически нива работи модулът?
2. Кой интерфейс използва: `I2C`, `SPI` или `UART`?
3. Има ли допълнителни control линии като `CS`, `RST`, `INT`, `BUSY` или `EN`?
4. Кой е най-малкият безопасен probe, който доказва, че връзката работи?

На `VK_RA4M2` има три особено важни практични ограничения:

- `I2C(0)` дели `P400` със `SW1`
- `I2C(1)` дели `P100/P101` със `hardware SPI`
- `UART(9)` е най-удобният отделен канал, когато I2C или SPI вече са заети

Това значи, че изборът на интерфейс не е само логически, а и ресурсен.

Кога кой интерфейс е най-подходящ:

- `I2C` е удобен за сензори и по-бавни модули с адресирана обща шина
- `SPI` е удобен за по-бързи модули, при които имате отделен `CS` и frame-базиран обмен
- `UART` е удобен за модеми, GPS, Bluetooth и други модули с текстов или байтов поток

Практическият ред на работа почти винаги е един и същ:

1. Свържете `GND` и проверете напрежението
2. Проверете дали логическите нива на модула са съвместими с борда
3. Свържете сигналните линии на интерфейса
4. Свържете и нужните control линии
5. Стартирайте с минимална скорост и минимален probe
6. Едва след това четете идентификатор, статус или payload

Минимален `I2C` probe:

```python
from machine import I2C  # Импортираме I2C за базов probe на външна шина.

i2c = I2C(1, freq=100000)  # Създаваме I2C(1) на безопасни 100 kHz.
print(i2c.scan())  # Сканираме шината за адреси на свързани I2C модули.
```

Минимална `SPI` транзакция:

```python
from machine import SPI, Pin  # Импортираме SPI и Pin за frame-базиран обмен с външен модул.

cs = Pin("P103", Pin.OUT, value=1)  # Държим CS неактивен преди началото на транзакцията.
spi = SPI(0, baudrate=500000, polarity=0, phase=0, bits=8, sck=Pin("P102"), mosi=Pin("P101"), miso=Pin("P100"), cs=cs)  # Създаваме SPI с консервативни настройки.
cs.value(0)  # Активираме target модула чрез CS.
spi.write_readinto(b"\x9F\x00\x00\x00", bytearray(4))  # Изпращаме команда и четем едновременно отговора.
cs.value(1)  # Освобождаваме модула след края на frame-а.
```

Минимален `UART` request/response:

```python
from machine import UART  # Импортираме UART за сериен обмен с външен модул.

uart = UART(9, 115200)  # Създаваме UART(9) с често срещана начална скорост.
uart.write(b"AT\r\n")  # Изпращаме примерна текстова команда към модула.
print(uart.read())  # Четем каквото е пристигнало към този момент.
```

Общата идея е проста:

- `I2C` първо се доказва със `scan()`
- `SPI` първо се доказва с правилен `CS` и пълен frame
- `UART` първо се доказва с правилен `baud rate`, `TX/RX` и timeout

След това вече се минава към конкретния регистров модел или към конкретния текстов протокол на модула.

Тук много по-важен от „готовия код“ е `datasheet`-ът:

- за `I2C` ви трябват адрес и регистри
- за `SPI` ви трябват `polarity`, `phase`, `CS` логика и формат на frame-а
- за `UART` ви трябват `baud rate`, формат на кадъра и синтаксис на командите

Файлове:

- `examples/34_external_modules/01_i2c_module_probe.py`
- `examples/34_external_modules/02_spi_module_transaction.py`
- `examples/34_external_modules/03_uart_module_request_response.py`

#### Резюме
- Главата събира `I2C`, `SPI` и `UART` в единен practically-first подход за интеграция на външни модули.
- Фокусът е върху захранване, логически нива, control линии и най-малкия възможен probe.
- На `VK_RA4M2` изборът на интерфейс е свързан и с конфликти между пинове и периферии.

#### Самопроверка
1. Защо при външен модул е грешно да започнете директно от голям driver, без минимален probe?
2. Кога бихте избрали `SPI` вместо `I2C` за външен модул?
3. Защо `UART(9)` често е най-удобният избор, когато вече ползвате `I2C(1)` или `SPI`?

#### Упражнения
1. Направете собствен checklist за свързване на нов външен модул с колони `power`, `pins`, `protocol`, `probe`.
2. Разширете `01_i2c_module_probe.py`, така че да чете два различни регистъра, ако адресът е намерен.
3. Добавете timeout и втори опит в `03_uart_module_request_response.py`, преди да обявите връзката за неуспешна.

#### Ако не работи
- Симптом: модулът мълчи напълно. Проверка: първо вижте `GND`, захранването и логическите нива, не кода.
- Симптом: има отговор, но той е безсмислен. Проверка: вижте адреса, `baud rate`, `SPI mode`, формата на командата и datasheet-а.
- Симптом: модулът тръгва само понякога. Проверка: проверете `RST`, `CS`, `BUSY`, `INT` и ресурсните конфликти между пиновете на борда.

#### Нататък
След тази глава преминете към „Основи на asyncio“, за да видите как външен модул може да се обслужва неблокиращо и като част от по-голямо приложение.

## Част XII: asyncio като учебна секция

### Глава 38. Основи на asyncio

Пример:

```python
import asyncio  # Импортираме asyncio за кооперативна многозадачност.
from machine import Pin  # Импортираме Pin за реален хардуерен достъп.

LED_ON = 0  # Active-low: 0 = светва LED1.
LED_OFF = 1  # Active-low: 1 = гаси LED1.

led = Pin("LED1", Pin.OUT, value=LED_OFF)  # LED1 = P204, започваме изгасен.
button = Pin("SW1", Pin.IN, Pin.PULL_UP)  # SW1 = P400, вътрешен pull-up.

async def blink_task(period_ms):  # Асинхронна задача за мигане с зададен период.
    while True:  # Безкрайно мигане.
        led.value(LED_ON)  # Светваме LED1.
        await asyncio.sleep_ms(period_ms)  # Отстъпваме управлението за period_ms.
        led.value(LED_OFF)  # Гасим LED1.
        await asyncio.sleep_ms(period_ms)  # Отстъпваме управлението отново.
```

Тази глава покрива:

- `create_task` за стартиране на задачи
- `asyncio.gather` за паралелно изпълнение
- три задачи едновременно: мигане, бутон, отчет

Файл:

- `examples/33_asyncio/01_asyncio_basics.py`

#### Резюме
- Главата систематизира `asyncio` чрез няколко кооперативни задачи.
- Показва се как LED, бутон и отчет могат да живеят като отделни роли в една програма.
- Това е реален практически модел за неблокиращи приложения върху платката.

#### Самопроверка
1. Какво прави `create_task` спрямо директно извикване на функция?
2. Защо `asyncio` е кооперативен, а не прекъсващ модел?
3. Кога бихте избрали този подход пред много блокиращи цикли?

#### Упражнения
1. Добавете нова async задача, която периодично логва състоянието на приложението.
2. Променете периода на мигане според някакво събитие или брояч.
3. Спрете една от задачите след timeout и проверете дали останалите продължават.

#### Ако не работи
- Симптом: event loop-ът „замръзва“. Проверка: някоя задача вероятно съдържа блокиращ код без `await`.
- Симптом: дадена задача сякаш не съществува. Проверка: вижте дали реално е създадена и дали отстъпва управлението.

#### Нататък
След тази глава преминете към „asyncio Event синхронизация“, за да надградите базовите задачи до координирано асинхронно поведение.

### Глава 39. asyncio Event синхронизация

Пример:

```python
import asyncio  # Импортираме asyncio за кооперативна многозадачност.
from machine import Pin  # Импортираме Pin за реален хардуерен достъп.

LED_ON = 0  # Active-low: 0 = светва LED1.
LED_OFF = 1  # Active-low: 1 = гаси LED1.

led = Pin("LED1", Pin.OUT, value=LED_OFF)  # LED1 = P204, започваме изгасен.
button = Pin("SW1", Pin.IN, Pin.PULL_UP)  # SW1 = P400, вътрешен pull-up.
button_event = asyncio.Event()  # Създаваме Event обект за синхронизация между задачи.

async def button_producer():  # Тази задача следи SW1 и сигнализира при натискане.
    last_value = 1  # Последна стойност на бутона (ненатиснат).
    current_value = button.value()  # Четем SW1.
    if current_value == 0 and last_value == 1:  # Нов натиск (falling edge).
        button_event.set()  # Сигнализираме Event-а.

async def led_consumer():  # Тази задача чака Event и мига LED1.
    await button_event.wait()  # Чакаме Event-а (блокираме кооперативно).
    button_event.clear()  # Нулираме Event-а за следващо използване.
```

Тук се вижда:

- `asyncio.Event` за комуникация между задачи
- производител-консуматор модел
- кооперативно блокиране без busy-wait

Файл:

- `examples/33_asyncio/02_asyncio_event_flag.py`

#### Резюме
- Главата показва `asyncio.Event` като чист механизъм за синхронизация между задачи.
- Моделът producer-consumer прави асинхронната комуникация по-ясна и по-надеждна.
- Това е важна крачка от „много задачи“ към „задачи, които си взаимодействат правилно“.

#### Самопроверка
1. Каква е ползата от `asyncio.Event` спрямо обикновен споделен флаг?
2. Защо консуматорът обикновено изчиства Event-а след обработка?
3. Как бихте разширили примера, ако няколко задачи трябва да реагират на едно събитие?

#### Упражнения
1. Добавете брояч на Event сработванията и го отпечатвайте периодично.
2. Създайте втори консуматор, който също чака същото събитие.
3. Добавете debounce или edge detection в producer задачата преди `set()`.

#### Ако не работи
- Симптом: реакцията се случва само веднъж. Проверка: вижте дали Event-ът отново се задава след `clear()`.
- Симптом: натискания се губят. Проверка: producer логиката и polling интервалът може да са твърде груби.

#### Нататък
След тази глава прегледайте каталога на примерите и се върнете към темите, които искате да доразвиете на реален борд.

## Част XIII: Специализирани светлинни модули

### Глава 40. NeoPixel

`NeoPixel` е удобен пример за „умен“ външен RGB светодиоден модул.

Той не се управлява като обикновен RGB LED с три PWM канала.

Вместо това се използва един data проводник с точно времедиаграмиране, което MicroPython крие зад модула `neopixel`.

В този курс използваме:

- `P500` като задължителен enable pin за модула в този setup
- `P112` като `DIN` data линия
- `1` пиксел за най-простия тест

Това е удобно за демонстрация, но има важно уточнение:

- `P500` трябва да бъде включен задължително в този setup
- при по-дълга лента или по-голям ток може да е нужно отделно основно захранване
- при отделно захранване масата пак трябва да е обща с борда

Ключовата идея е, че работата с `NeoPixel` минава през три стъпки:

1. Включвате захранването на модула
2. Записвате RGB стойност в буфера
3. Извиквате `write()`, за да се изпрати timed bitstream към пиксела

Минимален пример:

```python
from machine import Pin  # Импортираме Pin за power-enable и data линията.
import neopixel  # Импортираме neopixel драйвера за RGB модула.

vcc = Pin("P500", Pin.OUT, value=1)  # Включваме P500, защото без него модулът не е активен в този setup.
np = neopixel.NeoPixel(Pin("P112"), 1)  # Създаваме NeoPixel обект за един пиксел върху P112.
np[0] = (40, 0, 0)  # Зареждаме червено в буфера на първия пиксел.
np.write()  # Изпращаме буфера и реално сменяме цвета на модула.
```

Тук има две практични разлики спрямо обикновен GPIO или PWM пример:

- цветът се задава като кортеж `(R, G, B)`
- промяната не се вижда преди `write()`

Втори важен практически момент е яркостта.

В учебните примери стойностите са ниски, например `40`, за да се ограничи токът и да се намали рискът при първи тест.

Трети важен момент е, че `NeoPixel` е външен модул, така че физическото свързване е толкова важно, колкото и кодът:

- `DIN` на модула трябва да отиде към `P112`
- `P500` трябва да е включен
- захранването трябва да е стабилно
- ако се ползва отделно основно захранване, масата трябва да е обща с борда

Файлове:

- `examples/neopixel.py`
- `examples/neopixel_test.py`

#### Резюме
- Главата показва `NeoPixel` като специализиран външен RGB модул с еднопроводен data интерфейс.
- Основният practically-first модел е `power`, запис в буфера и `write()`.
- Ниската яркост и правилното захранване са критични за безопасен първи тест.

#### Самопроверка
1. Защо `NeoPixel` не се управлява като обикновен RGB LED с три отделни PWM канала?
2. Каква е ролята на `write()` след присвояване на `np[0] = (...)`?
3. Защо `P500` остава задължителен, дори ако за по-дълга лента добавите отделно основно захранване?

#### Упражнения
1. Увеличете броя пиксели на `2` или `4` и направете цветово преливане по лентата.
2. Добавете функция, която изгасва всички пиксели след определено време без действие.
3. Свържете `SW1` и сменяйте цвета на NeoPixel-а при всяко натискане.

#### Ако не работи
- Симптом: няма никаква светлина. Проверка: вижте дали `P500` реално подава захранване и дали `DIN` е към правилния вход на модула.
- Симптом: цветът е грешен или нестабилен. Проверка: намалете яркостта, скъсете проводниците и проверете общата маса.
- Симптом: единичният модул работи, а лентата не. Проверка: при повече пиксели осигурете отделно основно захранване, но оставете `P500` включен.

#### Нататък
След тази глава прочетете и следващата глава за `WS2812` през `machine.WS2812`, за да видите разликата между софтуерен bitstream и SCI-базиран TX-only подход.

### Глава 41. WS2812 през `machine.WS2812` и SCI TX-only

`WS2812B` е от същото семейство адресируеми RGB светодиоди, но в този порт вече има и втори начин за управление.

Вместо класическия `neopixel` модул с `machine.bitstream()`, може да се използва `machine.WS2812`.

Този драйвер кодира данните през подходящ `SCI` канал и извежда само data сигнала през избрания `TXD/MOSI` pin.

Практически това означава:

- към самия светодиоден модул свързвате само `DIN` към избрания data pin
- не подавате външен `SCK` и `MISO` към лентата
- продължавате да работите с буфер и `write()`, почти както при `NeoPixel`

От гледна точка на Python кодът е еднопроводен: има само data pin към модула.

От гледна точка на микроконтролера вътрешно се използва `SCI` хардуерът, така че избраният pin автоматично определя кой `SCI` блок ще бъде зает.

В текущата работеща реализация timing-ът вече не се прави с по-старото `3-bit` кодиране.

Сега драйверът използва:

- вътрешен `SCI` поток около `2.857143 MHz`
- `5-bit` кодиране за всеки логически WS2812 бит
- кратък скрит `LOW` prefix преди payload-а
- достатъчно дълъг `LOW` suffix за `latch/reset`
- непрекъснат `DTC` feed, за да няма байтови дупки в натоварена `IRQ` среда

Това е важно, защото от Python страна API-то си остава просто:

- пишете RGB стойности в буфера
- извиквате `write()`
- не мислите за ръчно bit timing управление

От практическа гледна точка е най-добре да започнете с дефолтните параметри на драйвера и да не задавате `baudrate`, освен ако нямате конкретна експериментална причина.

Поддържаните data pin-ове в този порт са:

- `SCI0` -> `P101`, `P205`, `P411`
- `SCI1` -> `P213`, `P401`, `P501`
- `SCI2` -> `P102`, `P112`, `P302`
- `SCI9` -> `P109`, `P203`, `P409`, `P602`

Това значи, че `P112` вече е default pin, но не е единствен възможен.

Важното ограничение е по канали, не само по pin.

Ако изберете pin от `SCI2` групата, не трябва едновременно да очаквате:

- `machine.WS2812`
- `UART(2)`
- `SPI(2)` през `SCI2`

Същата логика важи и за другите групи, ако вече ползвате същия `SCI` канал за друг интерфейс.

Минимален пример с алтернативен pin:

```python
from machine import Pin  # Импортираме Pin за захранването и data линията.
from machine import WS2812  # Импортираме специализирания драйвер за WS2812.
import time  # Импортираме time за кратко изчакване след включване на захранването.

vcc = Pin("P500", Pin.OUT, value=1)  # Включваме P500, защото без него модулът не е активен в този setup.
time.sleep_ms(100)  # Изчакваме power-enable линията и модулът да се стабилизират.
strip = WS2812(pixel_count=1, pin=Pin("P109"), channels=3)  # Създаваме драйверен обект върху алтернативния pin P109.
strip[0] = (40, 0, 0)  # Зареждаме червено в буфера на първия пиксел.
strip.write()  # Изпращаме буфера към модула през SCI TX-only backend-а.
```

Подобно на `NeoPixel`, и тук цветът не се вижда преди `write()`.

Разликата е, че тук timing-ът не идва от общия bitstream механизъм, а от специализиран C-side драйвер в порта.

Това е полезно по три причини:

- API-то е ясно вързано към `WS2812`
- можете да изберете удобен `SCI TX/MOSI` pin според реалното окабеляване
- логиката за кодиране, непрекъснато предаване и latch остава в C, а не в Python

Важно е и захранването:

- в този setup `P500` трябва да бъде включен задължително
- при повече пиксели може да е нужно отделно основно захранване, но `P500` остава включен
- масата на захранването и масата на борда трябва да са общи

Практически работен ред е:

1. Започнете с `pixel_count=1`.
2. Проверете последователно `red`, `green`, `blue`, `white`, `off`.
3. Едва след това минете към повече пиксели или матрица.

Важното ограничение е, че този драйвер е порт-специфичен.

Тоест примерите са силно полезни точно за `VK_RA4M2`, но не бива да очаквате същия `machine.WS2812` API на произволен друг MicroPython порт.

Файлове:

- `examples/ws2812_sci.py`
- `examples/ws2812_sci_test.py`
- `examples/ws2812_sci_chase_145.py`
- `examples/ws2812_sci_p101.py`
- `examples/ws2812_sci_p109.py`

#### Резюме
- Главата показва `WS2812` като специализиран `machine` клас, реализиран в този порт върху няколко `SCI TX/MOSI` pin групи.
- За самото свързване към модула е нужен само избраният data pin, без външен clock pin към светодиода.
- Конфликтите са по `SCI` канал: не може един и същ `SCI` блок да се дели едновременно от `WS2812` и `UART` или `SPI`.

#### Самопроверка
1. Каква е разликата между `neopixel.NeoPixel` и `machine.WS2812` в този порт?
2. Защо потребителят свързва само `DIN -> избрания TXD/MOSI pin`, въпреки че вътрешно драйверът използва `SCI`?
3. Какво общо имат `P109` и `UART(9)` при използване на `machine.WS2812`?

#### Упражнения
1. Пуснете един и същ пример веднъж с `P112`, а после с `P109`, и сравнете нужните промени в Python кода.
2. Променете `pixel_count` на `2` или `4` и направете цветен „марш“ по къса лента.
3. Изберете pin от `SCI0` или `SCI1` групата и проверете кои други интерфейси вече не бива да активирате едновременно.
4. Пуснете `examples/ws2812_sci_chase_145.py` върху `1 + 12*12` светодиода, като държите `P500` включен и осигурите достатъчно основно захранване, и наблюдавайте дали трите цвята се местят равномерно.

#### Ако не работи
- Симптом: няма никаква светлина. Проверка: вижте дали `P500` реално подава захранване и дали `DIN` е към правилния вход на модула.
- Симптом: получавате грешка за зает bus. Проверка: избраният `SCI` канал може вече да е зает от `UART` или `SPI` със същия номер.
- Симптом: цветовете са нестабилни при повече пиксели. Проверка: осигурете отделно основно захранване и обща маса, но не изключвайте `P500`.
- Симптом: няма светлина на матрицата. Проверка: уверете се, че `P500` е включен и че масата е обща.
- Симптом: `neopixel` примерът работи, а този не. Проверка: това е индикация, че точно избраният `SCI TX/MOSI` path трябва да се валидира на реален хардуер.

#### Нататък
След тази глава преминете към новата audio секция за `DAC` и `retro_synth`, за да видите как същият борд може да се използва не само за светлина, но и за синтез на звук.

## Част XIV: Звук, DAC и retro synth

### Глава 42. DAC audio, timed playback и retro synth

До този момент книгата разглежда аналогов вход чрез `ADC` и импулсен изход чрез `PWM`.

`VK_RA4M2` обаче има и истински `DAC` изход, което позволява да се генерира аналогов звуков сигнал директно от борда.

Това е особено полезно за:

- ретро звукови ефекти
- кратки мелодии
- арпежиа
- малки wavetable цикли
- game UI звуци тип `coin`, `jump`, `laser`, `explosion`

Важно е да мислите за този ресурс като за сигнален изход, а не като за изход за мощност.

Това означава:

- `P014 = DA0 = J19-5 = A4`
- `P015 = DA1 = J19-6 = A5`
- `J16` е boot-mode jumper и не е DAC изход
- за слушане ползвайте активна колона, усилвател или осцилоскоп
- не свързвайте директно говорител като товар към DAC пина

Ключовата идея на timed playback е, че Python не подава всеки семпъл ръчно.

Вместо това:

- `DAC.write_timed(...)` конфигурира възпроизвеждането
- `AGT` таймер дава равномерния sample clock
- `DTC` или `DMAC` мести данните от RAM към DAC регистъра
- CPU се ползва за setup, teardown и control-rate промени, а не за всеки отделен семпъл

На практика това вече е потвърдено на реален хардуер за:

- `DTC` circular playback
- `DTC` one-shot playback
- `DMAC` one-shot playback

Минимален timed DAC пример:

```python
from array import array
from machine import DAC, Pin
import time

wave = array("H", [2048, 3072, 4095, 3072, 2048, 1024, 0, 1024])  # 12-bit DAC стойности.
dac = DAC(Pin("P014"))  # Ползваме DA0 на J19-5 / A4.

dac.write_timed(
    wave,
    440 * len(wave),  # Един период на 440 Hz.
    mode=DAC.CIRCULAR,
    transfer=DAC.TRANSFER_DTC,
)

time.sleep_ms(500)
dac.stop()
```

От този пример трябва да останат три практични правила:

- семплите трябва да са в диапазон `0..4095`
- най-сигурният буфер е `array('H', ...)`
- looping режимът е през `DTC`, а one-shot burst ефектите могат да минат през `DMAC`

Защо лимитът `256` семпъла не е проблем?

За ретро synth обикновено буферът е един период на вълната, а не дълъг записан звук.

Честотата на тона идва от:

`sample_rate = note_frequency * table_length`

При `256` семпъла това означава:

- `220 Hz` -> `56320 Hz`
- `440 Hz` -> `112640 Hz`
- `880 Hz` -> `225280 Hz`

Това е напълно достатъчно за square, pulse, triangle, saw, noise burst и повечето retro game звуци.

За по-удобна работа примерите вече включват готов helper:

```python
from retro_synth import RetroSynth

synth = RetroSynth("P014")
synth.play_note("A4", 120, "pulse25")
synth.play_arpeggio("A3", (0, 4, 7, 12), step_ms=60, cycles=4, wave="triangle")
synth.coin()
synth.stop()
```

Този helper скрива ниското ниво и дава удобен API за:

- `play_note()`
- `play_sweep()`
- `play_arpeggio()`
- `play_melody()`
- `coin()`, `jump()`, `laser()`, `explosion()`

От учебна гледна точка това е много полезно, защото разделя задачата на две нива:

1. На ниско ниво разбирате как работи timed DAC path-ът.
2. На високо ниво строите игрово поведение и музика върху готов synth helper.

Файлове:

- `examples/retro_synth.py`
- `examples/dac_retro_sfx.py`
- `examples/dac_retro_music.py`
- `examples/dac_firmware_probe.py`
- `examples/dac_dmac_diag.py`

`dac_retro_sfx.py` е най-бързият начин да чуете, че audio path-ът работи.

`dac_retro_music.py` показва, че може да се строи и по-музикално поведение, а не само кратки burst ефекти.

`dac_firmware_probe.py` е важният пример, когато искате да докажете firmware-а, а не просто да чуете звук. Той проверява отделно `DTC circular`, `DTC one-shot` и `DMAC one-shot`.

`dac_dmac_diag.py` остава диагностичният инструмент, ако някога има проблем само в `DMAC` path-а.

#### Резюме
- `VK_RA4M2` вече има реално работещ timed DAC backend през `AGT + DTC/DMAC + DAC`.
- За ретро звук мислете в малки wavetable цикли и control-rate промени, не в дълги PCM файлове.
- `P014` и `P015` са DAC пиновете; `J16` не е DAC изход.
- `retro_synth.py` е удобният layer за учебни sound effects и кратки мелодии.

#### Самопроверка
1. Защо `DAC` пинът не трябва да се третира като изход за мощност към гол говорител?
2. Кога е по-удачно да ползвате `DTC` circular и кога `DMAC` one-shot?
3. Защо `256` семпъла са достатъчни за ретро synth, но не са добър модел за дълъг записан звук?

#### Упражнения
1. Пуснете `examples/dac_retro_sfx.py` първо на `P014`, после преместете примера към `P015` и сравнете резултата.
2. Променете `table_len` в `retro_synth.py` на `32`, `64`, `128` и `256` и чуйте как се променя характерът на звука.
3. Пуснете `examples/dac_firmware_probe.py` и запишете кои три timed playback режима минават успешно на вашия борд.

#### Ако не работи
- Симптом: няма никакъв звук. Проверка: вижте дали реално сте на `P014` или `P015`, а не на `J16`.
- Симптом: има напрежение, но почти не се чува. Проверка: DAC пинът не е усилвател; пробвайте с активна колона, усилвател или осцилоскоп.
- Симптом: получавате `ValueError` за sample или странно груб звук. Проверка: вижте дали буферът е `array('H', ...)` и дали стойностите са в `0..4095`.
- Симптом: получавате грешка за зает timer. Проверка: уверете се, че предишният playback е спрян с `stop()` и че не държите други AGT-базирани ресурси активни.
- Симптом: съмнявате се дали проблемът е в примера или във firmware-а. Проверка: пуснете `examples/dac_firmware_probe.py`.

#### Нататък
След тази глава вече имате добра основа да третирате борда не само като логически контролер, а и като източник на звук. Най-разумната следваща стъпка е да комбинирате `FSM`, таймери и `retro_synth.py` в собствена малка игра или звуков интерфейс.

## Част XV: Надеждни вградени програми

Тази част не добавя нова периферия.

Тя добавя нещо по-важно: как от отделни работещи примери се стига до система, която стартира подредено, оцелява при грешки и може да се поддържа.

Новите глави тук стъпват върху вече наличните примерни файлове и ги свързват в по-зряло инженерно поведение.

### Глава 43. `boot.py`, `main.py` и жизнен цикъл на програмата

Една от най-големите разлики между „пускам пример“ и „имам устройство“ е жизненият цикъл на програмата.

На практика трябва да си отговорите:

- какво се случва веднага след reset
- кой код е безопасен за много ранно стартиране
- кой код може да блокира
- къде се създават драйверите
- къде се стартира основната логика

Полезен минимален модел е:

```python
# boot.py
from machine import Pin

status_led = Pin("LED1", Pin.OUT, value=1)

def early_init():
    status_led.value(0)
    status_led.value(1)

early_init()
```

```python
# main.py
import app

app.run()
```

```python
# app.py
def run():
    print("Стартира приложението.")
    # Тук вече може да се създадат обекти, задачи и основен loop.
```

Практическото правило е:

- `boot.py` прави само ранна и безопасна инициализация
- `main.py` е тънка входна точка
- реалното приложение живее в отделни модули

Това разделение е важно, защото ако сложите твърде много логика в `boot.py`, диагностицирането става трудно, а recovery сценарият става по-рисков.

Свързани файлове:

- `examples/index.py`
- `examples/28_machine_misc/01_machine_info_and_id.py`
- `examples/28_machine_misc/03_sleep_modes.py`
- `examples/31_case_study/01_bathroom_control.py`

#### Резюме
- Жизненият цикъл на програмата трябва да е предвидим още от reset-а.
- `boot.py` е за ранна инициализация, а не за голяма бизнес логика.
- `main.py` трябва да е проста входна точка към реалните модули на приложението.

#### Самопроверка
1. Защо не е добра идея целият проект да бъде написан директно в `boot.py`?
2. Какво печелите, ако `main.py` само извика `app.run()`?
3. Кои операции са безопасни рано при стартиране и кои е по-добре да се отложат?

#### Упражнения
1. Разделете един от по-дългите ви примери на `boot.py`, `main.py` и `app.py`.
2. Добавете проста ранна индикация с `LED1`, която показва, че началната инициализация е приключила.
3. Направете вариант, в който `main.py` първо печата кратък banner с версия и ID на устройството.

#### Ако не работи
- Симптом: бордът влиза в цикъл от рестартирания. Проверка: преместете тежката логика от `boot.py` към `main.py` или `app.py`.
- Симптом: не е ясно къде се чупи програмата. Проверка: добавете кратки диагностични `print()` стъпки в ранния startup.

#### Нататък
След тази глава вече има смисъл да подредите проекта по файлове и роли, вместо да държите всичко в един дълъг скрипт.

### Глава 44. Организация на проект: модули, drivers, config и `pins.py`

След като има ясен жизнен цикъл, следващият въпрос е как да подредите кода така, че да не се превърне в дълъг и крехък файл.

Полезна малка структура е:

```text
/flash/
    boot.py
    main.py
    app.py
    config.py
    pins.py
    drivers/
    services/
```

Идеята е проста:

- `pins.py` описва board mapping-а
- `config.py` пази настройки и константи
- `drivers/` държи ниското ниво
- `services/` държи по-високото поведение
- `app.py` свързва всичко

Минипример:

```python
# pins.py
LED_STATUS = "LED1"
BUTTON_OK = "P400"
DAC_OUT = "P014"
```

```python
# config.py
BUTTON_SAMPLE_MS = 10
AUDIO_WAVE = "pulse25"
```

```python
# app.py
from machine import Pin
import pins
import config

status_led = Pin(pins.LED_STATUS, Pin.OUT, value=1)
print("Button sample period =", config.BUTTON_SAMPLE_MS)
```

Така пиновете и настройките вече не са „магически числа“, разпръснати по целия проект.

Свързани файлове:

- `examples/31_case_study/01_bathroom_control.py`
- `examples/31_case_study/02_two_fsms.py`
- `examples/34_external_modules/01_i2c_module_probe.py`
- `examples/retro_synth.py`

#### Резюме
- Подреденият проект разделя board mapping, настройки, драйвери и логика.
- `pins.py` и `config.py` намаляват риска от скрити зависимости.
- Това е най-лесният преход от демо файл към поддържим firmware проект.

#### Самопроверка
1. Каква е ролята на `pins.py` и защо е по-добре от разпръснати pin низове из проекта?
2. Какво трябва да влиза в `drivers/` и какво в `services/`?
3. Защо е полезно `app.py` да сглобява системата, вместо всичко да се създава навсякъде?

#### Упражнения
1. Извадете pin имената от един ваш пример в отделен `pins.py`.
2. Извадете тайминги и прагове в отделен `config.py`.
3. Разделете код, който говори директно с хардуер, от код, който взема решения.

#### Ако не работи
- Симптом: след смяна на пин нищо не тръгва. Проверка: вижте дали всички места вече ползват `pins.py`, а не старо твърдо кодирано име.
- Симптом: проектът има дублирани стойности. Проверка: съберете period-и, прагове и pin mapping на едно място.

#### Нататък
Когато кодът вече е подреден, следващата голяма стъпка е да мислите и в двоични данни, а не само в прости цели числа и низове.

### Глава 45. Байтове, `bytearray`, `struct`, `memoryview` и двоични протоколи

Много реални устройства не говорят с удобни Python обекти, а с поредици от байтове.

Това е нормално при:

- `UART` request/response протоколи
- `SPI` framed транзакции
- `I2C` регистрови устройства
- собствени binary packet формати

Минипример:

```python
import struct

frame = bytearray(6)
frame[0] = 0xAA
struct.pack_into("<H", frame, 1, 1023)
frame[3] = 7

checksum = 0
for b in memoryview(frame)[:-1]:
    checksum ^= b

frame[-1] = checksum
print(frame)
```

От този пример трябва да останат четири идеи:

- `bytearray` е променяем буфер от байтове
- `struct` дава точно binary представяне
- `memoryview` ви позволява да работите без излишни копия
- checksum или framing логиката трябва да е ясна и повторяема

Свързани файлове:

- `examples/20_language_basics/05_arrays.py`
- `examples/20_language_basics/07_pointers_equivalents.py`
- `examples/24_i2c/04_i2ctarget_memory.py`
- `examples/32_uart/01_uart_basic.py`
- `examples/34_external_modules/02_spi_module_transaction.py`
- `examples/34_external_modules/03_uart_module_request_response.py`

#### Резюме
- Реалните протоколи често работят с сурови байтове, не с удобни Python структури.
- `bytearray`, `struct` и `memoryview` са основният работен комплект за такива задачи.
- Добре описан пакет е по-важен от „хитър“ код.

#### Самопроверка
1. Каква е разликата между `bytes` и `bytearray`?
2. Защо `struct.pack_into()` е полезен при работа с фиксирани буфери?
3. Каква е ползата от `memoryview` при по-големи буфери?

#### Упражнения
1. Направете малък пакет с `header`, `payload length` и `checksum`.
2. Декодирайте обратно пакет чрез `struct.unpack_from()`.
3. Добавете прост XOR checksum към UART или SPI тестов пакет.

#### Ако не работи
- Симптом: числата изглеждат „обърнати“. Проверка: вижте дали endian-ът е правилен в `struct` формата.
- Симптом: буферът е с грешна дължина. Проверка: опишете кадъра на хартия byte по byte, преди да кодирате.

#### Нататък
След като пакетите са ясни, идва естественият въпрос как системата да се държи стабилно, когато комуникацията или периферията се провалят.

### Глава 46. Обработка на грешки, recovery и fail-safe поведение

При реално устройство грешките не са изключение, а част от нормалната работа.

Това важи особено за:

- външни модули
- кабели и конектори
- шумни входове
- временни bus грешки
- непълни отговори по `UART`

Полезен модел е:

```python
import time

def read_with_retry(reader, retries=3, delay_ms=50):
    for attempt in range(retries):
        try:
            return reader()
        except OSError:
            time.sleep_ms(delay_ms)
    return None
```

Още по-важно е какво правите след неуспеха:

- записвате последната грешка
- връщате системата в безопасно състояние
- решавате дали да повторите, да деградирате функционално или да спрете

За fail-safe състояние често е по-важно „нищо опасно да не стане“, отколкото „всичко да продължи на всяка цена“.

Свързани файлове:

- `examples/34_external_modules/01_i2c_module_probe.py`
- `examples/34_external_modules/02_spi_module_transaction.py`
- `examples/34_external_modules/03_uart_module_request_response.py`
- `examples/27_storage/03_dataflash_state_led.py`

#### Резюме
- Грешките трябва да са очаквана част от проекта, а не странична мисъл.
- Полезните инструменти са retry, timeout, диагностика и fail-safe поведение.
- Recovery логиката трябва да е ясна и ограничена, а не безкрайно повторение без контрол.

#### Самопроверка
1. Каква е разликата между retry и fail-safe поведение?
2. Защо безкрайните повторения често са лоша стратегия?
3. Какво бихте записали като „последна грешка“, за да помогнете на диагностика?

#### Упражнения
1. Добавете retry логика към един I2C или UART пример.
2. Добавете флаг `last_error` и отпечатване на смислено съобщение.
3. Дефинирайте кое е безопасното състояние за вашия проект при провал на външен модул.

#### Ако не работи
- Симптом: проектът „замръзва“ при проблемен модул. Проверка: вижте дали липсва timeout или limit на повторенията.
- Симптом: след грешка системата остава в хаотично състояние. Проверка: добавете ясен `enter_safe_state()` блок.

#### Нататък
След тази глава вече има смисъл да обсъдите и watchdog идеята, но честно спрямо текущите възможности на порта.

### Глава 47. Watchdog и самовъзстановяване

Watchdog-ът е класически embedded инструмент: ако приложението спре да „диша“, системата се рестартира.

Важно уточнение за тази книга:

- като хардуерна идея watchdog е много ценен
- в текущия `renesas-ra` порт `WDT` не е поддържан

Затова тук темата е учебна и архитектурна, а не готов runtime пример за този борд.

Правилният мисловен модел е:

- имате основен „жив“ loop или scheduler
- имате ясни здравни точки, на които системата доказва, че е работоспособна
- watchdog се „храни“ само ако тези точки са минати успешно

Добър преди-watchdog pattern е софтуерно здравно следене:

```python
import time

last_alive_ms = time.ticks_ms()

def mark_alive():
    global last_alive_ms
    last_alive_ms = time.ticks_ms()

def looks_stuck(timeout_ms=2000):
    return time.ticks_diff(time.ticks_ms(), last_alive_ms) > timeout_ms
```

Това не е хардуерен watchdog, но учи на правилната дисциплина:

- къде системата доказва, че е жива
- кога трябва да се мине в safe state
- как да се подготви бъдеща миграция към реален `WDT`

Свързани файлове:

- `examples/01_blink_async.py`
- `examples/02_two_tasks.py`
- `examples/31_case_study/01_bathroom_control.py`
- `examples/33_asyncio/01_asyncio_basics.py`

#### Резюме
- Watchdog е важна embedded концепция, но в текущия `renesas-ra` порт не е наличен като поддържан runtime ресурс.
- Полезно е още сега да се мисли в health points, stuck detection и controlled recovery.
- Софтуерният health monitor е добра подготовка за бъдещ истински watchdog.

#### Самопроверка
1. Защо watchdog не трябва да се „храни“ просто по таймер, без реална проверка за здравето на системата?
2. Какво бихте използвали като признак, че приложението е живо?
3. Каква е честната разлика между watchdog концепция и текущата поддръжка на порта?

#### Упражнения
1. Добавете `last_alive_ms` логика към едно ваше приложение.
2. Направете LED индикация, която показва „жив“ режим и „грешка“ режим.
3. Опишете на хартия къде бихте поставили `WDT.feed()`, ако този ресурс стане наличен по-късно.

#### Ако не работи
- Симптом: системата изглежда „жива“, но не върши полезна работа. Проверка: health point-ът може да е поставен на грешното място.
- Симптом: recovery логиката се активира прекалено често. Проверка: вижте дали timeout-ът е реалистичен за най-бавния ви нормален сценарий.

#### Нататък
След надеждността на програмата идва следващата голяма тема: как реалният сигнал да стане по-чист и по-стабилен.

## Част XVI: Сигнали, измерване и калибрация

Тази част е мост между „кодът тръгва“ и „системата работи добре“.

Тук фокусът вече е върху качеството на сигналите, измерването и разумната работа в ограничен енергиен режим.

### Глава 48. Филтриране, калибриране и хистерезис при входни сигнали

Сензорният вход почти никога не е идеален.

Дори когато `ADC.read()` работи коректно, ще видите:

- шум
- кратки отклонения
- температурен дрейф
- нестабилност около прагове

Полезен модел е комбинация от:

- просто филтриране
- калибрация
- хистерезис

Минипример:

```python
def moving_average(samples):
    return sum(samples) // len(samples)

def hysteresis_state(value, state, low_th=1800, high_th=2200):
    if state == 0 and value >= high_th:
        return 1
    if state == 1 and value <= low_th:
        return 0
    return state
```

Идеята е:

- филтърът намалява шума
- калибрацията превежда raw стойности към полезен диапазон
- хистерезисът спира „тракането“ около границата

Свързани файлове:

- `examples/22_analog/01_analog_input_adc.py`
- `examples/23_timing/07_adc_timing_measure.py`
- `examples/26_touchpad/01_touchpad_basic.py`
- `examples/26_touchpad/02_touchpad_diagnostics.py`

#### Резюме
- Чистият входен сигнал рядко идва „готов“ от хардуера.
- Филтрирането, калибрацията и хистерезисът са нормална част от реалното приложение.
- Стабилното решение е по-ценно от максимално чувствителното.

#### Самопроверка
1. Защо един праг без хистерезис често води до нестабилно поведение?
2. Каква е ролята на moving average филтъра?
3. Каква е разликата между raw стойност и калибрирана стойност?

#### Упражнения
1. Добавете moving average върху ADC пример.
2. Добавете двоен праг и сравнете поведението с единичен праг.
3. Запишете минимална и максимална измерена стойност и направете проста калибрация към `0..100%`.

#### Ако не работи
- Симптом: стойността „прескача“. Проверка: вижте дали window-ът за филтър не е прекалено малък.
- Симптом: реакцията е прекалено бавна. Проверка: филтърът може да е прекалено тежък за тази задача.

#### Нататък
Следващата стъпка е да измервате не само логиката, но и реалния електрически сигнал навън.

### Глава 49. Измерване и диагностика с осцилоскоп и logic analyzer

Когато една периферия „уж работи“, но резултатът е съмнителен, софтуерният `print()` често не е достатъчен.

Тогава идват:

- осцилоскопът
- logic analyzer-ът
- измерванията с `ticks_us()`

Един много полезен навик е да отбелязвате важни моменти с debug pin:

```python
from machine import Pin

probe = Pin("P204", Pin.OUT, value=1)

def mark():
    probe.value(0)
    probe.value(1)
```

Така можете да видите:

- кога влиза callback
- колко често идва периодично събитие
- има ли jitter
- съвпада ли очакваният timing с реалния сигнал

Тази глава е особено важна за:

- `PWM`
- `UART`
- `SPI`
- `I2C`
- `DAC`
- debounce и button timing

Свързани файлове:

- `examples/23_timing/07_adc_timing_measure.py`
- `examples/23_timing/08_interrupt_latency_measure.py`
- `examples/23_timing/13_hardware_timer_periodic_oneshot.py`
- `examples/32_uart/01_uart_basic.py`
- `examples/dac_firmware_probe.py`
- `examples/dac_dmac_diag.py`

#### Резюме
- Истинската диагностика често изисква измерване на пина, не само четене на лог.
- Debug pin и `ticks_us()` са мощна комбинация за базова инструментализация.
- Осцилоскопът и logic analyzer-ът намаляват догадките и ускоряват root cause анализа.

#### Самопроверка
1. Кога `print()` е недостатъчен за timing проблем?
2. Какво печелите от кратък debug pulse върху отделен pin?
3. Кога е по-подходящ осцилоскоп и кога logic analyzer?

#### Упражнения
1. Добавете debug pin marker към един timer callback.
2. Измерете период и jitter на периодичен таймер.
3. Наблюдавайте един UART frame или DAC waveform на реален инструмент.

#### Ако не работи
- Симптом: измерването обърква самия експеримент. Проверка: не печатайте тежки съобщения вътре в чувствителен callback.
- Симптом: виждате шумен или странен сигнал. Проверка: проверете общата маса, probe настройката и bandwidth limit-а на инструмента.

#### Нататък
След като умеете да измервате и диагностицирате, идва въпросът как да спестите енергия, без да загубите контрол над системата.

### Глава 50. Ниска консумация и събуждане

При много бордови приложения системата не трябва да работи „на пълна газ“ непрекъснато.

Полезната идея е:

- работете кратко и целенасочено
- спете, когато няма работа
- събуждайте се по време или по събитие

Минипример:

```python
import machine
import time

print("Подготвяме работа.")
time.sleep_ms(20)
machine.lightsleep(50)
print("Събудихме се.")
```

Практическият ред на мислене е:

- първо овладейте кратък и безопасен `lightsleep`
- после измерете какво се запазва и какво не
- едва след това експериментирайте по-смело с по-дълбоки режими

За `VK_RA4M2` това трябва да се прави предпазливо, защото `lightsleep` и `deepsleep` вече са показани в книгата като чувствителни за допълнителна хардуерна валидация.

Свързани файлове:

- `examples/28_machine_misc/03_sleep_modes.py`
- `examples/23_timing/06_rtc_basic.py`
- `examples/30_fsm/08_tickless_sleep.py`

#### Резюме
- Ниската консумация е въпрос на архитектура, не само на една функция.
- Започвайте с кратки, контролирани `lightsleep` експерименти.
- Преди по-дълбок сън винаги проверявайте кои ресурси и състояния оцеляват след wake-up.

#### Самопроверка
1. Защо е по-разумно да започнете с кратък `lightsleep`, а не директно с агресивен `deepsleep`?
2. Каква е ролята на `RTC` в power-aware архитектурата?
3. Кои части от приложението трябва да умеят да се инициализират отново след събуждане?

#### Упражнения
1. Измерете реално колко време трае кратък `lightsleep`.
2. Направете приложение, което работи на интервали и спи между тях.
3. Опишете кои обекти във вашия проект трябва да се създават наново след reset или deep sleep.

#### Ако не работи
- Симптом: след wake-up поведението е различно от очакваното. Проверка: вижте кои обекти разчитат на старо състояние.
- Симптом: режимът е нестабилен. Проверка: върнете се към по-кратък и по-безопасен `lightsleep` сценарий.

#### Нататък
След тази част вече има добра основа да мислите не само за отделни ресурси, а за цялото приложение като наблюдаема система.

## Част XVII: Архитектура на реален проект

Тук книгата вече преминава от „познаване на API-та“ към „сглобяване на система“.

Именно тази част превръща демонстрационните примери в проектен начин на мислене.

### Глава 51. Състояние на системата: heartbeat, health checks и debug интерфейс

Когато проектът порасне, вече не е достатъчно да знаете само дали „нещо свети“.

Трябва да знаете:

- живо ли е приложението
- кога последно е имало грешка
- кой модул изостава
- какво е текущото състояние

Полезен минимален модел е:

```python
health = {
    "loops": 0,
    "last_error": None,
    "audio_active": False,
}

def heartbeat_tick():
    health["loops"] += 1
```

Това може да се комбинира с:

- LED heartbeat
- периодичен status print
- UART debug команда
- броячи за грешки и retries

Свързани файлове:

- `examples/01_blink_async.py`
- `examples/02_two_tasks.py`
- `examples/31_case_study/01_bathroom_control.py`
- `examples/32_uart/01_uart_basic.py`
- `examples/dac_firmware_probe.py`

#### Резюме
- Системното състояние трябва да бъде наблюдаемо, не скрито.
- Heartbeat, counters и `last_error` са прости, но много полезни инструменти.
- Диагностиката трябва да е част от проекта, а не спешна добавка след проблем.

#### Самопроверка
1. Какво е heartbeat и какво различава от обикновено мигане?
2. Кои три status полета биха били най-полезни за вашия проект?
3. Защо `last_error` е по-полезен от просто „Exception occurred“?

#### Упражнения
1. Добавете `health` речник към един ваш проект.
2. Направете периодичен статус print веднъж на секунда.
3. Добавете LED шаблон, който показва нормален режим и грешка.

#### Ако не работи
- Симптом: диагностиката пречи на времечувствителна логика. Проверка: преместете тежките съобщения извън callback-и и IRQ.
- Симптом: има твърде много логове. Проверка: ограничете печата по период или по събитие.

#### Нататък
След като системата е наблюдаема, идва естествената стъпка да капсулирате отделните ресурси като ясно дефинирани обекти.

### Глава 52. Капсуловане на периферии като класове

Класът не е само „обектно програмиране заради самото него“.

В бордови проект той е удобен начин да съберете на едно място:

- хардуерен ресурс
- локално състояние
- помощни методи
- ясна граница между driver и application logic

Минипример:

```python
from machine import Pin

class StatusLed:
    def __init__(self, pin_name="LED1"):
        self.pin = Pin(pin_name, Pin.OUT, value=1)

    def on(self):
        self.pin.value(0)

    def off(self):
        self.pin.value(1)
```

Тази идея вече се вижда и в `retro_synth.py`: ниското ниво е скрито зад по-чисто API.

Свързани файлове:

- `examples/retro_synth.py`
- `examples/26_touchpad/02_touchpad_diagnostics.py`
- `examples/31_case_study/02_two_fsms.py`

#### Резюме
- Класовете помагат да съберете състояние и поведение на едно място.
- Това прави проектите по-четими, по-лесни за тест и по-лесни за повторна употреба.
- Най-голямата полза е ясната граница между хардуерен детайл и логика на приложението.

#### Самопроверка
1. Какво печелите, ако даден периферен ресурс е капсулиран в клас?
2. Къде е границата между клас-драйвер и клас-услуга?
3. Защо глобалните променливи стават проблем при по-голям проект?

#### Упражнения
1. Превърнете един LED или бутон пример в клас.
2. Превърнете ADC четене с праг в клас с метод `is_active()`.
3. Изнесете повтаряща се логика от два примера в общ helper клас.

#### Ако не работи
- Симптом: класът изглежда по-сложен от скрипта. Проверка: класът трябва да събира смислено състояние, не просто да обвива един ред код.
- Симптом: логиката е размазана между много обекти. Проверка: разграничете driver слой от application layer.

#### Нататък
Когато класовете са на място, следващият естествен въпрос е как да пазите настройки, preset-и и таблици извън самия код.

### Глава 53. Таблични данни, конфигурации и малки файлови формати

Не всяка промяна в проекта трябва да изисква редакция на Python сорса.

Често е по-добре да отделите:

- настройки
- прагове
- preset-и
- мелодии
- таблици за състояния

Прост и полезен формат е `JSON`:

```python
import json

cfg = {
    "button_sample_ms": 10,
    "audio_wave": "triangle",
    "melody_tempo": 120,
}

with open("/flash/config.json", "w") as f:
    json.dump(cfg, f)
```

Това е особено удобно, когато:

- искате различни режими без промяна на кода
- пазите калибрационни стойности
- съхранявате малки preset-и за звук или светлина

Свързани файлове:

- `examples/27_storage/01_flash_blockdev.py`
- `examples/27_storage/02_dataflash_basic.py`
- `examples/27_storage/03_dataflash_state_led.py`
- `examples/retro_synth.py`

#### Резюме
- Конфигурацията трябва да е отделена от логиката, когато има шанс да се променя.
- Малки файлови формати като `JSON` са удобни за настройки и preset-и.
- Табличният подход прави проектите по-гъвкави и по-лесни за поддръжка.

#### Самопроверка
1. Кои стойности във ваш проект са конфигурация, а не логика?
2. Кога е по-добре да ползвате файл вместо твърдо кодирана константа?
3. Какви данни не е разумно да пазите в прекалено сложен формат на такъв малък проект?

#### Упражнения
1. Изнесете прагове, delay-и или имена на пинове в `JSON` файл.
2. Направете два различни preset-а и превключване между тях.
3. Запишете калибрационни стойности във `flash` и ги зареждайте при старт.

#### Ако не работи
- Симптом: програмата пада при четене на config. Проверка: добавете разумни стойности по подразбиране при липсващ или повреден файл.
- Симптом: config файлът расте неконтролируемо. Проверка: разделете настройки, telemetry и user data.

#### Нататък
Остава финалната интеграционна глава: как от поредица примери се стига до завършено приложение.

### Глава 54. От пример към завършено приложение

Това е най-важната „мета“ глава в края на книгата.

Тя събира всичко дотук в един практичен ред:

1. Изберете минимален работещ пример.
2. Потвърдете хардуерния ресурс на борда.
3. Изнесете pin mapping и config.
4. Създайте малки драйвери или helper класове.
5. Добавете status и error reporting.
6. Добавете fail-safe и recovery стратегия.
7. Едва след това разширявайте поведението.

Полезен skeleton е:

```python
def init_board():
    pass

def init_services():
    pass

def run_forever():
    while True:
        pass

def main():
    init_board()
    init_services()
    run_forever()

main()
```

Това изглежда просто, но точно тук се ражда качественият firmware проект:

- ясна последователност
- ясни модули
- ясни точки за диагностика
- ясни места за бъдещо разширение

Свързани файлове:

- `examples/31_case_study/01_bathroom_control.py`
- `examples/31_case_study/02_two_fsms.py`
- `examples/33_asyncio/01_asyncio_basics.py`
- `examples/retro_synth.py`
- `examples/dac_retro_music.py`

#### Резюме
- Завършеното приложение не е „голям пример“, а подредена система.
- Най-стабилният път е да растете от доказан минимален пример към все по-структуриран проект.
- Диагностика, конфигурация и recovery трябва да са част от архитектурата, а не следмисъл.

#### Самопроверка
1. Кой е най-рисковият преход между „примерът работи“ и „проектът е готов“?
2. Кои три архитектурни стъпки бихте добавили веднага към следващия си проект?
3. Как бихте разбрали, че проектът е вече „подреден“, а не просто „още работещ“?

#### Упражнения
1. Изберете един пример от книгата и го преработете в малък проект със структура `config`, `pins`, `app`.
2. Добавете `health` състояние, `last_error` и безопасно спиране.
3. Направете кратък release checklist за собственото си приложение.

#### Ако не работи
- Симптом: проектът работи само като един голям файл. Проверка: разделете startup, drivers и основна логика.
- Симптом: всяка нова функция чупи старата. Проверка: липсват граници между модулите и стабилни точки за проверка.

#### Нататък
След тази глава вече имате не само набор от работещи примери, а и модел как от тях да построите цялостно приложение за реален борд.

## Каталог на всички примери

Този каталог е финалната проверка, че нито един пример не е пропуснат в книгата.

- `examples/01_blink_async.py` : асинхронно мигане на `LED1`
- `examples/02_two_tasks.py` : две едновременни `asyncio` задачи
- `examples/03_pwm_ab_same_channel.py` : два PWM изхода върху един GPT канал
- `examples/04_touchpad_async.py` : асинхронно следене на TouchPad с cached API
- `examples/dac_dmac_diag.py` : DMAC регистрова диагностика за timed DAC path-а
- `examples/dac_firmware_probe.py` : директна проверка на `DTC` и `DMAC` timed DAC firmware path-а
- `examples/dac_retro_music.py` : кратка мелодия и arpeggio bed върху DAC
- `examples/dac_retro_sfx.py` : готов demo за `coin`, `jump`, `laser`, `explosion` върху DAC
- `examples/neopixel.py` : NeoPixel rainbow cycle с `P500` захранване и `P112` data линия
- `examples/neopixel_test.py` : кратък NeoPixel smoke test с основни цветове и изгасване
- `examples/retro_synth.py` : reusable helper за ноти, sweep, arpeggio и ретро звукови ефекти
- `examples/ws2812_sci.py` : WS2812 rainbow cycle през `machine.WS2812` и `SCI2` върху `P112`
- `examples/ws2812_sci_test.py` : кратък WS2812 smoke test през `machine.WS2812`
- `examples/ws2812_sci_chase_145.py` : три движещи се цвята върху `1 + 12*12` WS2812 светодиода
- `examples/ws2812_sci_p101.py` : WS2812 rainbow cycle през `machine.WS2812` върху алтернативния `SCI0` pin `P101`
- `examples/ws2812_sci_p109.py` : кратък WS2812 smoke test през `machine.WS2812` върху алтернативния `SCI9` pin `P109`
- `examples/20_language_basics/01_sample_program.py` : първа примерна програма
- `examples/20_language_basics/02_variables_and_constants.py` : променливи и константи
- `examples/20_language_basics/03_operators_conditions_loops.py` : оператори, условия и цикли
- `examples/20_language_basics/04_enums.py` : enum-подобни стойности с `const()`
- `examples/20_language_basics/05_arrays.py` : списъци, вложени таблици, `array`, `bytearray`
- `examples/20_language_basics/06_structures.py` : `dict`, `namedtuple`, вложени структури
- `examples/20_language_basics/07_pointers_equivalents.py` : references, `memoryview`, `machine.mem32`
- `examples/20_language_basics/08_functions.py` : функции, callbacks и таблици от функции
- `examples/21_digital_io/01_digital_output.py` : цифров изход с `LED1`
- `examples/21_digital_io/02_digital_input.py` : цифров вход с `SW1`
- `examples/22_analog/01_analog_input_adc.py` : ADC вътрешни и външни канали
- `examples/22_analog/02_pwm_output.py` : базов PWM изход
- `examples/23_timing/01_delays_and_timers.py` : блокиращо закъснение, хардуерен таймер, `uasyncio`
- `examples/23_timing/02_interrupts.py` : GPIO прекъсвания и защитено четене
- `examples/23_timing/03_button_debounce.py` : дебаунс с polling и с `uasyncio`
- `examples/23_timing/04_software_timer_minus1.py` : софтуерен `Timer(-1)`
- `examples/23_timing/05_timer_compare_hw_sw.py` : сравнение между `Timer(1)` и `Timer(-1)`
- `examples/23_timing/06_rtc_basic.py` : RTC основи
- `examples/23_timing/07_adc_timing_measure.py` : измерване на `ADC.read()` с `time.ticks_us()`
- `examples/23_timing/08_interrupt_latency_measure.py` : ориентировъчно измерване на GPIO interrupt latency
- `examples/23_timing/09_button_polling_events.py` : `polling` четене на бутон със `PRESS`, `RELEASE`, `CLICK` и `LONG_PRESS`
- `examples/23_timing/10_button_irq_deferred.py` : бутон по прекъсване с отложена debounce обработка
- `examples/23_timing/11_button_soft_timer_hold.py` : `Timer(-1)` sampler с `PRESS`, `RELEASE`, `HOLD_START` и `HOLD_REPEAT`
- `examples/23_timing/12_button_shift_debounce.py` : debounce чрез битово изместване и shift-register история
- `examples/23_timing/13_hardware_timer_periodic_oneshot.py` : хардуерен `Timer.PERIODIC`, `Timer.ONE_SHOT`, `period()` и `counter()`
- `examples/23_timing/14_hardware_timer_output_compare.py` : `output compare` callback-и за `channel(1)` и `channel(2)`
- `examples/23_timing/15_hardware_timer_input_capture.py` : `input capture` за период и `pulse width`
- `examples/23_timing/16_hardware_timer_event_count.py` : `event count` и callback на всеки `N` импулса чрез базовия timer callback
- `examples/23_timing/17_fast_irq_two_buttons_queue.py` : два бутона с `fast ASM IRQ`, shared ring buffer и печат извън прекъсването
- `examples/AGT_TEST_CHECKLIST.md` : практичен runtime checklist за AGT таймерния слой на борда
- `examples/24_i2c/01_i2c_master_basic.py` : базов I2C master
- `examples/24_i2c/02_i2c_runtime_pins.py` : I2C с runtime зададени пинове
- `examples/24_i2c/03_softi2c_basic.py` : SoftI2C
- `examples/24_i2c/04_i2ctarget_memory.py` : I2CTarget с memory-backed буфер
- `examples/25_spi/01_spi_basic.py` : hardware SPI
- `examples/25_spi/02_softspi_basic.py` : SoftSPI
- `examples/26_touchpad/01_touchpad_basic.py` : базов TouchPad
- `examples/26_touchpad/02_touchpad_diagnostics.py` : диагностика, tuning и cached API
- `examples/27_storage/01_flash_blockdev.py` : `renesas.Flash` и `/flash`
- `examples/27_storage/02_dataflash_basic.py` : `dataflash` модул
- `examples/27_storage/03_dataflash_state_led.py` : устойчиво състояние в `dataflash` и LED индикация
- `examples/28_machine_misc/01_machine_info_and_id.py` : `machine.info`, `freq`, `unique_id`
- `examples/28_machine_misc/02_machine_mem_and_bootloader.py` : `disable_irq`, `enable_irq`, `mem32`, `bootloader`
- `examples/28_machine_misc/03_sleep_modes.py` : `sleep`, `lightsleep`, `deepsleep`
- `examples/28_machine_misc/04_memory_management.py` : `gc`, `micropython.mem_info`, `bytearray`, `memoryview`
- `examples/29_display_scan/01_dynamic_7seg_and_keypad.py` : динамична индикация и матрично сканиране
- `examples/30_fsm/01_fsm_lamp_button.py` : минимална FSM
- `examples/30_fsm/02_fsm_table.py` : таблична FSM
- `examples/30_fsm/03_fsm_timeout.py` : FSM с timeout
- `examples/30_fsm/04_conditions_vs_events.py` : conditions срещу events
- `examples/30_fsm/05_guard_functions.py` : guard функции
- `examples/30_fsm/06_priorities.py` : приоритети между събития
- `examples/30_fsm/07_event_queue.py` : event queue
- `examples/30_fsm/08_tickless_sleep.py` : tickless идея и sleep
- `examples/31_case_study/01_bathroom_control.py` : баня с няколко сензора и реален LED1/SW1
- `examples/31_case_study/02_two_fsms.py` : лампа и вентилатор като две FSM с реален LED1/SW1
- `examples/32_uart/01_uart_basic.py` : базова UART комуникация и loopback тест
- `examples/34_external_modules/01_i2c_module_probe.py` : I2C scan и безопасен probe на външен модул
- `examples/34_external_modules/02_spi_module_transaction.py` : SPI framed transaction с `CS` и анализ на суровия отговор
- `examples/34_external_modules/03_uart_module_request_response.py` : UART request/response с timeout към външен модул
- `examples/33_asyncio/01_asyncio_basics.py` : asyncio основи с три паралелни задачи
- `examples/33_asyncio/02_asyncio_event_flag.py` : asyncio Event синхронизация
- `examples/index.py` : скрипт за листване на всички примери на борда

## Какво още не е потвърдено на реален борд

Книгата и примерите са прегледани за пълност и са подредени спрямо плана.

Още не е потвърдено пълно runtime изпълнение на всички примери върху реален `VK_RA4M2`.

Вече са потвърдени на реален борд:

- timed DAC playback през `DTC` circular
- timed DAC playback през `DTC` one-shot
- timed DAC playback през `DMAC` one-shot
- `examples/dac_retro_sfx.py`
- `examples/dac_retro_music.py`

Най-чувствителните места остават:

- външните I2C устройства
- SPI loopback и външни SPI модули
- външните UART модули и AT-подобни текстови протоколи
- NeoPixel модулът върху `P500/P112`, особено при по-дълги проводници или повече от един пиксел
- WS2812 path-ът през `machine.WS2812` и различните `SCI TX/MOSI` pin-ове, защото е нов порт-специфичен backend и трябва да се потвърди на реален хардуер
- 7-сегментната индикация и матричната клавиатура
- write режимите на `Flash` и `dataflash`
- `lightsleep` и `deepsleep`

## Заключение

Тази версия на книгата е построена като проверка за пълност:

- всички теми от плана са отразени
- всички примерни файлове от каталога са споменати изрично
- всяка голяма тема има поне един реален кодов откъс
- порт-специфичните ограничения на `VK_RA4M2` са записани ясно
- преходът от демонстрационен пример към подреден firmware проект вече е разгледан в отделни архитектурни глави

Следващата разумна стъпка вече е двустепенна:

- хардуерна валидация на примерите по групи върху реалната платка
- постепенно добавяне на отделни нови `.py` примери, които да илюстрират по-практично новите архитектурни глави от части XV, XVI и XVII
