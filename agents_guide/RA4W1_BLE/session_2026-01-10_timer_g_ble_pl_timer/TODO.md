Session TODO (timer / g_ble_pl_timer)

- Determine expected type/usage of g_ble_pl_timer from libr_ble.a (rf_cmt.o).
- Implement and wire a real timer instance and assign g_ble_pl_timer.
- Ensure IRQ / callback paths work with MicroPython RA port.
- Build + run minimal BLE init smoke check.

